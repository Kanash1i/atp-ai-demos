# 08 · 被测系统速查（mock-shop）

写案例时对照查的东西：能用哪些数据、页面上有哪些元素、变量怎么写。

> 全部为**虚构合成**数据。被测系统是仓库自带的 `atp-platform/mock-shop`，
> 不访问任何真实站点。

## 访问地址

| 地址 | 谁在用 |
|---|---|
| `http://192.168.0.101:25488` | 人（局域网） |
| `http://100.75.32.82:25488` | 人（Tailscale，出门也能开） |
| `http://mock-shop:8088` | 执行节点与页面探查（compose 网络内） |

⚠️ **平台配置里的 `MOCK_SHOP_URL` 必须填「执行机视角」的地址**（`http://mock-shop:8088`）。
平台自己连不到被测系统是正常的——按拓扑只有执行机能连。
填成 `localhost:8088` 会让页面探查全挂，而报错出现在三跳之外的节点上
（`ERR_CONNECTION_REFUSED`）。这个坑真撞过。

---

## 账号

| 邮箱 | 密码 | 昵称 | 状态 | 用途 |
|---|---|---|---|---|
| `tanaka@example.jp` | `Passw0rd!` | 田中 直樹 | 正常 | 绝大多数案例 |
| `locked@example.jp` | `whatever` | ロック済み | 锁定 | 锁定提示与解锁引导 |

> ### ⚠️ 密码里的 `0` 是数字零
>
> ```
> P  a  s  s  w  0  r  d  !
>                ↑
>          数字零 U+0030，不是字母 o
> ```
>
> 打成字母 o 会得到 `ユーザー名またはパスワードが正しくありません`——
> 而系统**不区分「用户不存在」和「密码错误」**（有意为之，不泄露账号是否注册过），
> 所以从提示上看不出错在哪。这个坑真踩过。

**只有这两个账号存在。** 其余任何邮箱都走「用户不存在」分支，返回同一句提示。
写断言时别指望能区分这两种情况。

---

## 页面地图

| 路由 | 页面 | 说明 |
|---|---|---|
| `/` · `/home` | 首页 | |
| `/login` | 登录 | 「记住我」决定登录态存 localStorage 还是 sessionStorage |
| `/mypage` | 会员中心 | 显示昵称与登录方式 |
| `/password/reset` | 密码重置 | 需带 `?token=`，值用 `${reset_token}` |
| `/sso` · `/sso/apps/{app}` | SSO 门户 | |
| `/sso/launch` | SSO 跳转 | 从门户点进来的落地页 |
| `/products/{id}` | 商品详情 | id 用 `P100` / `P200` / `P300` |
| `/cart` | 购物车 | 数量改写、优惠券、缺货拦截都在这 |
| `/checkout` | 结算 | 选地址 → 确认 → 下单，三段式 |
| `/orders` | 订单列表 | 带状态筛选 |
| `/orders/{orderNo}` | 订单详情 | 支付 / 取消 / 退货申请的入口 |

**页面是 JS 动态渲染的。** 直接 `curl` 拿到的 HTML 里只有一个
`<div id="app">`，元素全是运行时生成——这正是 agent 必须靠页面探查、
而不能读源码猜定位器的原因。

---

## 变量与凭据

案例里**不要写死账号和 URL**，用变量占位。执行时由节点注入实际值。

> ### ⚠️ 两套语法，别混用
>
> | 写法 | 用途 | 日志里 |
> |---|---|---|
> | `${xxx}` | 普通变量：账号、URL、商品号、订单号 | **原样打印** |
> | `@cred{xxx}` | 凭据：口令、token | **脱敏成 `***`** |
>
> 口令用错语法 = 把口令写进了执行日志。

### 普通变量 `${…}`

| 变量名 | 实际值 |
|---|---|
| `base_url` | `http://mock-shop:8088` |
| `test_user` | `tanaka@example.jp` |
| `test_user_nickname` | 田中 直樹 |
| `lockout_test_user` | `locked@example.jp` |
| `sso_portal_url` | `${base_url}/sso` |
| `reset_token` | `demo-reset-token-0001` |
| `sample_product_id` | `P100` |
| `valid_coupon_code` | `SAVE1000` |
| `pending_order_no` | `ORD-20260830-0001` |
| `delivered_order_no` | `ORD-20260829-0002` |

### 凭据 `@cred{…}`

| 变量名 | 实际值 |
|---|---|
| `test_user_password` | `Passw0rd!` |
| `lockout_wrong_pwd` | `whatever` |
| `reset_new_password` | `NewPassw0rd!` |

口令来自环境变量注入（仓库根 `.env`），不写死在配置里。

> ### ⚠️ 变量名拼错不报错，会当成字面量
>
> `${sample_prodcut_id}` 少一个字母，Playwright 就会去找一个名叫
> 「`${sample_prodcut_id}`」的商品，然后超时失败——
> **报错指向「元素找不到」，不是「变量不存在」**。

---

## 商品

| ID | 名称 | 价格 | 库存 | 用途 |
|---|---|---|---|---|
| `P100` | ワイヤレスイヤホン | 12,800 | 有货 | 默认商品 |
| `P200` | スマートウォッチ | 24,000 | 有货 | 需要第二件时用 |
| `P300` | モバイルバッテリー | 3,980 | **缺货** | 结算时会被拦下并高亮 |

**购物车初始状态是三件商品各 1 个**（含那件缺货的）。
所以「直接去结算」这类案例默认会撞上缺货拦截——要测正常下单，
先把 P300 移除，或者用 `cart-clear` 清空后重新加购。

小计 40,780 ＋ 送料 800 ＝ 合计 41,580。

---

## 订单

| 单号 | 日期 | 金额 | 状态 | 可做什么 |
|---|---|---|---|---|
| `ORD-20260830-0001` | 2026-08-30 09:12 | 12,800 | PENDING | 支付、取消 |
| `ORD-20260829-0002` | 2026-08-29 15:40 | 24,000 | DELIVERED | 退货申请（可传图） |
| `ORD-20260828-0003` | 2026-08-28 11:05 | 3,980 | CANCELLED | 终态，验「操作按钮不该出现」 |

收货地址三单相同：`東京都渋谷区神宮前1-2-3　田中 直樹`。

⚠️ 状态是**写在前端的种子数据**，存 localStorage。案例跑完改了状态，
下一条案例看到的是改过的。依赖初始状态的案例要么自己先重置，要么别假设。

---

## 优惠券

| 码 | 减免 | 说明 |
|---|---|---|
| `SAVE1000` | −1,000 | 对应 `${valid_coupon_code}` |
| `WELCOME500` | −500 | 需要第二个有效券时用 |

只有这两个码有效。合计算法是 `小计 + 送料 − 折扣`，且**不低于 0**——
写「折扣大于总额」的边界案例时，断言要写成合计等于 0，不是负数。

---

## 动作与定位器

支持的动作类型（只能用这些）：

```
OPEN_URL   CLICK   INPUT   SELECT
ASSERT_TEXT   ASSERT_VISIBLE   ASSERT_NOT_EXIST
WAIT_FOR   SCROLL_TO   SWITCH_FRAME   SWITCH_WINDOW   UPLOAD   SLEEP
```

| 定位器类型 | 等待条件 |
|---|---|
| `CSS` `XPATH` `ID` `NAME` `LINK_TEXT` | `NONE` `PRESENCE` `VISIBLE` `CLICKABLE` |

页面元素基本都带 `data-testid`，所以 `XPATH` + `//tag[@data-testid='…']`
是最稳的写法。

⚠️ STD-004 要求异步操作后必须显式等待，`SLEEP` 硬等会被校验器拦下（ERROR 档）。

---

## 一条完整案例

```
// ATP-LOGIN-0001  正常ログイン
1. OPEN_URL     "${base_url}/login"
2. INPUT        XPATH //input[@data-testid='login-username']  ← "${test_user}"
3. INPUT        XPATH //input[@data-testid='login-password']  ← "@cred{test_user_password}"
4. CLICK        XPATH //button[@data-testid='login-submit']       CLICKABLE
5. WAIT_FOR     XPATH //span[@data-testid='user-nickname']        VISIBLE
6. ASSERT_TEXT  XPATH //span[@data-testid='user-nickname']  = "${test_user_nickname}"   VISIBLE
```

**你不需要自己找定位器。** agent 有页面探查工具——它会让执行节点真的打开页面、
抓下可交互元素，再挑定位器。你只要说清楚**要测什么**和**用哪套数据**。

探查和执行是同一台机器，所以探到的 DOM 就是执行时会看到的 DOM。

---

## 元素清单

各页面的 `data-testid`。列在这里是方便核对 agent 给的定位器对不对，
**写案例时不用手抄**。

| 页面 | data-testid |
|---|---|
| 登录 / 全局 | `login-form` `login-username` `login-password` `login-submit` `login-error` `login-method` `remember-me` `username-field-error` `unlock-guide-link` `home-header` `main-nav` `nav-cart` `cart-badge-count` `user-menu-toggle` `user-nickname` `logout-button` `session-expired-dialog` `relogin-button` `redirect-hint` |
| 商品详情 | `product-detail` `product-name` `product-price` `product-stock` `add-to-cart` `add-to-cart-toast` |
| 购物车 | `cart-table` `cart-row` `cart-item-count` `cart-item-qty` `cart-qty-update` `cart-item-subtotal` `cart-item-remove` `cart-subtotal` `cart-shipping-fee` `cart-discount` `cart-grand-total` `coupon-code` `coupon-apply` `coupon-applied-badge` `cart-checkout` `cart-clear` `cart-empty` `cart-error` `continue-shopping` `confirm-dialog-ok` |
| 结算 | `address-option` `address-add-new` `address-name` `address-zip` `address-detail` `address-phone` `address-save` `selected-address` `checkout-next` `order-confirm-panel` `confirm-items` `confirm-total` `order-submit` `order-no` `order-status` |
| 订单 | `order-list` `order-row` `order-no` `order-status` `order-detail-link` `order-status-filter` `order-filter-apply` `order-pay` `pay-method-credit` `pay-method-conveni` `pay-submit` `order-cancel` `cancel-reason` `cancel-confirm` `order-return-apply` `return-reason` `return-detail` `return-photo` `uploaded-filename` `return-submit` `return-no` |
| 会员中心 | `mypage-profile` `profile-nickname` `profile-login-method` |
| 密码重置 | `reset-token` `new-password` `new-password-confirm` `reset-submit` `reset-error` `reset-success` |
| SSO | `sso-app-launch` |

> ### ⚠️ 页面探查只看得到「当前这一帧」
>
> 实测：探查 `/cart` 抓到 23 个候选，`cart-checkout` / `cart-clear` /
> `coupon-apply` 都在，但 **`confirm-dialog-ok` 抓不到**——
> 它是点了「清空」之后才出现的确认对话框，此刻 `display:none`，
> 被可见性过滤器滤掉了。
>
> 交互后才出现的元素（对话框、下拉展开项、条件渲染的错误提示）**全部抓不到**。
> agent 写这类案例时只能猜或者问用户，猜错要等真跑才发现。
>
> 要补的话有两条路：探查时支持传一个前置动作序列（先点几下再抓），
> 或者把不可见元素也带上但标注 `hidden`。前者更准，后者更省事。

---

## 数据来源

| 内容 | 文件 |
|---|---|
| 账号 / 商品 / 订单 / 优惠券 | `atp-platform/mock-shop/src/main/resources/static/js/` |
| 页面路由 | `atp-platform/mock-shop/src/main/java/com/atp/mock/MockShopApplication.java` |
| 变量与凭据 | `atp-platform/atp-runner/src/main/resources/application.yml` |
| 动作 / 定位器 / 等待条件枚举 | `atp-platform/atp-common/src/main/java/com/atp/common/enums/` |
| 八条规范的判据 | `atp-platform/atp-common/src/main/java/com/atp/common/validation/StandardsValidator.java` |
