<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <!-- ⚠️ 刻意不继承 spring-boot-starter-parent。
       这是个被 agent 高频反复调用的 CLI，冷启动是真实成本：
       Spring Boot ≈ 1.5s / picocli fat jar ≈ 300ms。一次会话调 20 次差 24s。
       理由见 06-atp-cli-设计.md §1。 -->

  <groupId>dev.kanashi.atp</groupId>
  <artifactId>atp-cli</artifactId>
  <version>0.1.0-SNAPSHOT</version>
  <packaging>jar</packaging>

  <name>ATP 案例编写 CLI (demo2)</name>
  <description>把幂等键做成老平台案例表的主键，用唯一约束 + CAS UPDATE 当并发仲裁点</description>

  <properties>
    <maven.compiler.release>21</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

    <picocli.version>4.7.6</picocli.version>
    <jackson.version>2.18.2</jackson.version>
    <postgresql.version>42.7.4</postgresql.version>
    <jsonschema.version>1.5.4</jsonschema.version>

    <junit.version>5.11.4</junit.version>
    <assertj.version>3.26.3</assertj.version>
    <docker.api.version>1.44</docker.api.version>
    <testcontainers.version>1.21.3</testcontainers.version>
  </properties>

  <dependencies>
    <dependency>
      <groupId>info.picocli</groupId>
      <artifactId>picocli</artifactId>
      <version>${picocli.version}</version>
    </dependency>
    <dependency>
      <groupId>com.fasterxml.jackson.core</groupId>
      <artifactId>jackson-databind</artifactId>
      <version>${jackson.version}</version>
    </dependency>
    <dependency>
      <groupId>com.networknt</groupId>
      <artifactId>json-schema-validator</artifactId>
      <version>${jsonschema.version}</version>
    </dependency>
    <dependency>
      <groupId>org.postgresql</groupId>
      <artifactId>postgresql</artifactId>
      <version>${postgresql.version}</version>
    </dependency>

    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <version>${junit.version}</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.assertj</groupId>
      <artifactId>assertj-core</artifactId>
      <version>${assertj.version}</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>postgresql</artifactId>
      <version>${testcontainers.version}</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>junit-jupiter</artifactId>
      <version>${testcontainers.version}</version>
      <scope>test</scope>
    </dependency>
    <!-- ⚠️ 不是 test scope。json-schema-validator 带了 slf4j-api 却没有 provider，
         缺 provider 时它会往 stderr 打三行 warning —— 那会混进 agent 读的输出里。
         主程序用 simplelogger 且只打 ERROR（src/main/resources/simplelogger.properties）；
         测试用 src/test/resources 下同名文件覆盖成 info，好看 Testcontainers 的诊断。 -->
    <dependency>
      <groupId>org.slf4j</groupId>
      <artifactId>slf4j-simple</artifactId>
      <version>2.0.16</version>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <!-- fat jar：bin/atp 直接 java -jar 它。刻意不用 spring-boot-maven-plugin。 -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-shade-plugin</artifactId>
        <version>3.6.0</version>
        <executions>
          <execution>
            <phase>package</phase>
            <goals><goal>shade</goal></goals>
            <configuration>
              <finalName>atp</finalName>
              <createDependencyReducedPom>false</createDependencyReducedPom>
              <transformers>
                <transformer implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                  <mainClass>dev.kanashi.atp.cli.AtpCli</mainClass>
                </transformer>
                <transformer implementation="org.apache.maven.plugins.shade.resource.ServicesResourceTransformer"/>
              </transformers>
              <filters>
                <filter>
                  <artifact>*:*</artifact>
                  <excludes>
                    <exclude>META-INF/*.SF</exclude>
                    <exclude>META-INF/*.DSA</exclude>
                    <exclude>META-INF/*.RSA</exclude>
                    <exclude>module-info.class</exclude>
                  </excludes>
                </filter>
              </filters>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>3.5.2</version>
        <configuration>
          <!-- ⚠️ 不要删。Docker Engine 29 起最低只接受 API 1.40，
               而 Testcontainers 依赖的 docker-java 默认仍谈判到 1.32，
               表现为 "Could not find a valid Docker environment"，看不出真因。
               docker-java 从系统属性 api.version 取值（环境变量 DOCKER_API_VERSION 无效，实测）。
               详见 DECISIONS.md D-101。 -->
          <argLine>-Dapi.version=${docker.api.version}</argLine>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>
