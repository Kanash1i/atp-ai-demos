# 后端 origin。dev 时 vite 把 /api 代理到这里；不填默认 http://localhost:8080
VITE_API_ORIGIN=http://localhost:8080

# 前端实际请求的前缀。默认空字符串 = 走同源 /api（dev 下由上面的代理转发，
# 生产下前后端同域）。只有在**不经代理、直连别的域**时才需要设，
# 那种情况后端必须把该来源加进 CORS 白名单。
# VITE_API_BASE=

# M1 还没接 Sa-Token，当前用户由查询参数带。三个演示账号：kaneshiro / sato / tanaka
VITE_DEMO_USER=kaneshiro
