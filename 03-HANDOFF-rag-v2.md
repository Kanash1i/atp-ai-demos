# 交接文档 — ATP 知识助手 v2（换引擎重做）

> ## ⚡ 开工前状态（2026-08-27 实测，直接可用）
>
> ### 服务机三条冒烟 —— 全过，不需要重跑
>
> | 检查 | 结果 |
> |---|---|
> | ① GPU | `tei-embed` / `tei-rerank` 均为 **`model on Cuda`**（不是 Cpu）|
> | ② embedding | `BAAI/bge-m3`，实测 **1024 维**，`max_client_batch_size=32` |
> | ③ rerank 方向 | `BAAI/bge-reranker-v2-m3`，相关文档 **0.3422**，无关的 0.0152 / 0.0001 ✓ |
>
> `192.168.0.101:8081` / `:8082` 均返回 200，MinerU `:8000` 也在跑。
> **不需要走 `RERANK_ENABLED=false` 的降级路线，消融表可以把 rerank 那一档跑全。**
>
> > ⚠️ 曾经有一段时间 `:8082` 返回 404（端口上听着的不是 TEI），现在已恢复。
> > 每次开工前仍然要跑这三条 —— 服务 health 200 不代表模型跑在 GPU 上，本项目踩过。
>
> ### ⭐ 一个已经量到的数，会影响你怎么设阈值
>
> rerank 的分值范围**很小**：相关 0.34、无关 0.015 / 0.0001。
> 引擎里默认的 `minScore(0.6)` 这类**绝对阈值会把所有结果清零**。
> 必须改用相对阈值或按排名截断 —— 详见 `04-M0-engine-audit.md`。
> **这是静默失败：不报错，只是召回变空。**
>
> ### 生产侧（demo2）已完工，不要再动它
>
> `atp` CLI 已合入 main（**Go + cobra + pgx**，27 个用例绿）。
> M3 完整规则引擎与 M5 清理任务**刻意押后** —— 演示不依赖，
> 而**知识侧的消融表才是核心交付物**。开场文档是 `07-CLI-项目综述.md`。
>
> ### demo1 的资产在哪
>
> 旧源码（Java 8 + langchain4j）已于 2026-08-27 **物理删除**，git 历史里可回溯。
> 语料、决策记录、三处参考实现保留在 **`archive/demo1/`**，
> 各留了什么、为什么，见该目录的 `README.md`。
>
> ### 硬纪律（复述一遍，因为 demo1 就是栽在这条上）
>
> **语料一灌进去、检索一能跑，下一件事就是评估集和 baseline 数字**
> —— 不是加功能、不是调 prompt、不是做 UI。**时间不够时砍功能，不砍评估。**

> **给新 session 的第一条指令**：读完本文档 + `00-SHARED-CONTEXT.md` 即可开工，
> 不需要回头问上一个 session。demo1 的源码**已物理删除**，只有 `archive/demo1/` 里的语料与素材还在。
> 那份代码已归档，本文档已把该带走的东西全部提炼进来。
>
> 本文档写于 2026-08-19，承接已删除的 `01-HANDOFF-demo1-rag.md`（内容已提炼进本文）。

---

## 0. 一句话背景

demo1（Java 8 + langchain4j，全手搓）做到 M3，**核心交付物消融表一行没跑出来**。
现在改为：**买来一个成熟的 Java 21 RAG 引擎（汽车领域，已在台式机验证跑通），
把它改造成 ATP 知识助手**。世界观、语料、评估设计、面试主线全部沿用 demo1，只换底层引擎。

**这不是从头再来。** demo1 攒下的东西里，代码只占很小一部分，
真正值钱的是踩坑清单、评估方法论和一整套面试叙事 —— 那些跟框架无关，全部有效。

---

## 1. 为什么换：demo1 的结局要说清楚

### 1.1 做完了什么

| 里程碑 | 状态 | 产出 |
|---|---|---|
| M0 Java 8 链路 spike | ✅ | JDK8 → TEI → Qdrant 全通，四项自检 |
| M1 语料生成 | ✅ | 15 篇文档 + 80 条案例，中日混排，含刻意植入的脏数据 |
| M2 入库 pipeline | ✅ | 多 collection、多切分策略、图片链路 |
| M3 基础检索 + CLI | ✅ | 路由 + rerank + XPath lint 通道，带召回详情 |
| M3e 多格式（PDF/DOCX） | ✅ | 手搓 PDF outline 切块，三格式 chunk 数 199 完全一致 |
| **M4 评估框架** | ❌ | `eval/` 是空目录 |
| **M5 消融表** | ❌ | README 里 7 行全是 `—` |

### 1.2 为什么归档 —— 诚实的复盘

**不是技术上做不下去，是把预算花错了地方。**

demo1 在两件事上超支：

1. **Java 8 的约束链**。锁 langchain4j 0.35 → 逼着 Qdrant 降到 v1.11.5 → 逼着 Spring Boot 停在 2.7.18。
   每一环都要实测字节码、都要写决策记录。这条链贡献了 26 条 DECISIONS 里的一半。
2. **手搓 PDF/DOCX 解析**（D-022~D-025）。为了让 `HEADING_PATH` 在 PDF 上不退化成 `FIXED`，
   移植了 Spring AI 的 outline 切块算法、自己写 PDF 生成器造语料、
   处理 CJK 字体嵌入、图片 CTM 坐标 —— **四条决策记录，全部是文档解析的脏活。**

这些工作本身没做错，每一条都留下了真实的踩坑素材。
但 CLAUDE.md 第一天就写了「**砍功能，不砍评估**」，而实际发生的恰恰相反：
功能做了一堆，评估一行没跑。**这是 demo1 最大的教训，也是新项目第一条纪律。**

### 1.3 换引擎解决了什么

买来的项目直接消掉了上面两条超支：

| demo1 的负担 | v2 的状况 |
|---|---|
| Java 8 → langchain4j 0.35 → Qdrant 1.11.5 整条约束链 | **Java 21，链条整个消失** |
| 手搓 PDF outline 切块（D-023），扫描件无解 | **MinerU 转 markdown**，版面分析走视觉模型 |
| 意图识别自己写规则 + LLM 兜底（D-011/D-016） | 引擎已实现 |
| 混合检索需自实现 `EmbeddingStore`（D-002 方案 B，留给 M5 从未做） | 引擎已实现，含重排序 |

**所以 v2 的工作量集中在「换语料 + 补评估」，而不是「搭链路」。**
这正好把预算逼回消融表上。

---

## 2. 带走的经验（与框架无关，写给下一个项目）

这一节是本文档最该被读第二遍的部分。七条，按可复用程度排序。

### 2.1 ⭐ 把会静默失败的地方变成显式检查

demo1 踩到的坑有个共同特征：**系统照常运行，只是结果悄悄错了。**

| 坑 | 表现 | 真实后果 |
|---|---|---|
| TEI 退化到 CPU | 容器正常、`/health` 200、正常返回 1024 维向量 | 14 核满载，GPU 利用率 0%，**靠 CPU 风扇声才发现** |
| Qdrant proto 字段迁移 | 命中、score、payload 全对，只有向量是空的 | 报错指向「向量长度不一致」，根因却在 server 版本 |
| slf4j provider 版本错配 | 不报错 | 日志**全部消失** |
| rerank 响应未排序，按顺序读 | 照常返回分数 | 分数配错文档，消融表整张被污染 |
| rerank 阈值用绝对值 | 案例检索**静默返回 0 条** | B 类指标归零，看起来像「检索能力差」 |
| `optional:` 配置源缺失 | 不报错，占位符当字面值传下去 | 报错出现在完全无关的地方 |
| `MarkdownDocument.flush()` 条件写错 | 不报错 | 15 篇文档的引言全被丢掉，1464 字符凭空消失 |
| PDF 抽图不注册 CTM 操作符 | 不报错 | 所有图片位置算成页面底部，全归到最后一节 |

**通用做法**：任何「拿到了结果但没法确认结果对不对」的环节，都要有一个能证伪的判据。
- GPU：不看「容器里能不能 `nvidia-smi`」，看**框架日志的 device 字段 + 显存增量**
- 检索：入库后点数对、维度对、payload 对都不算数，**必须真查一次**
- 版本兼容：把检查放进**绕不过去的构造路径**，不要只放在 spike 里

面试被问「你怎么保证检索质量」时，**「我把所有会静默失败的地方都变成了显式检查」**
比罗列技术栈有力得多。

### 2.2 ⭐ 异构内容共用一条打分链路时，任何「按分数比较」的地方都要先问一句「这两个分数可比吗」

同一个根因在 demo1 里咬了三次，前两次都在打补丁，第三次才看出是一个问题：

| 位置 | 表现 | 修法 |
|---|---|---|
| 阈值截断 | 案例检索静默返回 0 条 | 绝对阈值 → `max(绝对下限, top1 × 比例)` |
| 路由开关 | 路由判错就整类召回不到 | 开关 → **配额**（判为不相关的一侧仍查，只是 topK 降低） |
| 结果排序 | 案例被文档凭高分挤掉 | 全局排序 → **按组分配名额**，主类占多数 |

根因：reranker 对「自然语言 query vs 结构化步骤序列」的相关性打分天然偏低。
文档类 top1 落在 0.59~0.99，案例类只有 0.008~0.38 —— **差 1~2 个数量级**。

**这条在 v2 依然成立**，而且买来的引擎是汽车领域的（大概率只有一种内容形态），
一旦灌进 ATP 语料的「手册 + 结构化案例」双形态，这个问题**必然复现**。
灌数据后第一件事就是量一下两类内容的 rerank 分数分布。

> 附带收获：RRF（倒数排名融合）正好优雅地绕开这个问题 —— 它只用排名不用分数。
> 买来的引擎如果混合检索用的是 RRF，这一层已经免疫；如果用的是加权分数融合，那要小心。

### 2.3 ⭐ 评估先于功能 —— demo1 就是栽在这条上

**新项目的硬纪律**：语料一灌进去、检索一能跑，**下一件事就是评估集和 baseline 数字**，
不是加功能、不是优化 prompt、不是做 UI。

demo1 的顺序是「M1 语料 → M2 入库 → M3 检索 → M3e 多格式 → …… → M4 评估」，
结果 M3e 之后精力耗尽，M4 永远没开始。
**v2 的顺序必须是「灌语料 → 评估集 → baseline → 才允许加任何东西」。**

配套的两条方法论：

- **优先做纯规则能算的指标**。拒答用固定标记 `[资料不足]` + `contains` 判定；
  引用准确率用「回答里引了 `[7]` 但只给了 5 条资料」这种可机器检测的幻觉信号。
  LLM-as-judge 在 40 条规模上又慢又贵又不稳，而且引出「judge 自己谁来评」的新问题。
  面试说**「我知道 LLM-as-judge，但在这个规模下引用准确率是更可靠的代理指标」**比堆评分体系更显清醒。
- **评估必须能不经过生成就跑**。检索指标（Recall/MRR/nDCG）不需要 LLM，
  这样跑消融不烧 token、不受模型波动影响。
  代价是不能用框架把 RAG 全链路包成一个返回 String 的黑盒（demo1 因此放弃了 `AiServices`）。

### 2.4 评估集的 D 类要取光谱两端，避开边界地带

这是 demo1 最没预料到的一个发现，**光看代码和语料都想不到，是实测撞出来的**。

「应拒答」不是两种，是一条光谱：

| 提问 | 语料状况 | 正确行为 | 稳定性 |
|---|---|---|---|
| 「ATP 支持 App 自动化吗」 | FAQ 明确写了不支持 | 正常答「不支持」+ 引用 | ✅ 稳定 |
| 「ASSERT_JSON 怎么用」 | 语料声明 action 集合**封闭** | ？ | ❌ **两轮结果不同** |
| 「执行器线程池怎么配」 | 完全没覆盖 | 输出 `[资料不足]` | ✅ 稳定 |

中间那条的成因：《Action 参考》末尾有一句元陈述「Action 枚举是封闭的，
如果你在别处看到一个不在这张表里的 action 名字，那它不存在」——
**这一句话把「语料没提到」变成了「语料间接否定了」**，于是它跨到了「有依据」那一侧，但只是勉强跨过去。

**两个后果，都要带进 v2**：

1. **拒答标记的语义必须收窄到只有一件事**。最初 prompt 写的是「资料不足**或者**该功能不支持，就输出标记」——
   这个「或者」让指标的分子里混了两类东西，其中一类答对了反而被算成失败，**指标会奖励一个更差的系统**。
   改成：标记只表示「我没有资料所以答不了」，并显式写明「资料明确说不支持时，那是有依据的答案，不要输出标记」。
2. **40 条规模下，一条边界用例的抖动就能造出几个百分点的假象**，看起来像是「某项优化有效/无效」。
   D 类只取「明确否定」和「完全无覆盖」两端；边界用例单独标注、不计入主指标。

### 2.5 参数要量过再说

被问「chunkSize 和 overlap 的依据是什么」，当时的答案是「700 是从 512 token 倒推的，80 是拍的」——
这个答案在面试里站不住。去量了语料之后发现了更要紧的事：

```
小节长度（199 个）：中位数 174、p75 246、p90 347、p95 407、max 610 字符
```

```
chunkSize   HEADING_PATH 块数   FIXED 块数
   700            89              41
  1200            89              25      ← 加到 1200，HEADING_PATH 纹丝不动
```

因为最长的小节才 610 字符，全都短于 700，**overlap 那段循环从来没被执行过**。
**在主策略下 chunkSize 和 overlap 是死参数**，只在 baseline 那一行真正生效。

这不是 bug，是结构感知切分的本意：**让 chunk 边界由文档的语义结构决定，而不是由一个拍出来的数字决定。**

**面试该这么答**：
> 「在我的语料上这两个参数对主策略不起作用 —— 小节 p95 是 407 字符，而上限设的 700，从来没触发过切分。
> 实际的 chunk 粒度是文档的标题结构决定的。参数只在 baseline 那一行生效。」

这比「我设了 700」强得多，因为它显示**量过**。而且自然引出下一个问题：
换一份长文档语料（真实的平台手册一节可能几千字），这两个参数就会开始起作用，那时才需要扫参。

> 顺带：overlap 的实测结论是「有边界感知的切分时基本是纯成本」。
> 切点由「找换行边界」决定，与 overlap 无关；overlap 只决定下一块从哪开始读，
> 而且那个位置是纯算术回退、不过边界判断 —— 于是块首会出现 `xt()="提交"]` 这种碎片。
> **建议 v2 的 baseline 和主策略都设 overlap=0**，否则 baseline 背着一个额外负担，
> 「结构感知切分」那一行的提升会被高估（消融表不公平）。

### 2.6 替换实现时，除了对齐功能，还要专门过一遍「上一版为了防什么而写的代码」

demo1 把手写的 178 行 `Env` 换成 Spring Boot 的 `spring.config.import` + `@ConfigurationProperties`，
轮子去掉了，但**手写那版里区分「路径问题 vs 配置漏写」的报错被弄丢了，而且退化得更糟**：

配置源缺失时，未解析的占位符被当字面值一路传下去，最后炸在

```
java.lang.IllegalArgumentException: Expected URL scheme 'http' or 'https' but no colon was found
```

—— okhttp 构造 embedding 客户端时抛的。人会去查 URL 拼接、查 TEI 服务、查 okhttp 版本，
唯独想不到是 `.env` 没找到。

**这条对 v2 极其相关**：整个 v2 就是一次大规模的「替换实现」。
买来的引擎替掉了 demo1 的切分、路由、检索、解析 —— 每一处都要问一句
**「demo1 在这里加过什么加固？新引擎有没有？」**
这类代码往往没有测试覆盖，因为它防的是环境问题，不是逻辑问题。

本文档 §2.1 那张表就是清单，逐条对照。

### 2.7 不要照抄上游实现

demo1 移植 Spring AI 的 PDF outline 切块时，有三处不能照抄，其中一处会**静默出错**：

```
书签的 top（PDPageXYZDestination）   原点在左下角，Y 向上
PDFTextStripperByArea 的 Rectangle  原点在左上角，Y 向下
```

必须 `Rectangle.y = pageHeight - pdfTop`。原版是直接填的。
照抄会把区域搬到页面的另一半，抽出来的文本张冠李戴 —— **而且不报错**。

**推广到 v2**：买来的项目是别人为汽车领域写的。
它的每一个阈值、每一个正则、每一条意图规则，都是在**别人的语料**上调出来的。
换成 ATP 语料（中日混排 + 结构化案例）之后，哪些还成立要**逐个验证**，不能默认继承。
尤其注意：意图识别的关键词表、rerank 的截断阈值、混合检索的融合权重。

> 补充一条 demo1 的实例：信号词表最初写了「支持吗」，
> 而「ATP 支持 App 自动化吗」中间隔了字，匹配不上，白白退化成全查。
> **信号词必须是连续子串**，而且**信号词表要用评估集来校准，不能凭想象列**。

---

## 3. 可直接搬走的资产（带路径）

归档目录：**`archive/demo1/`**（源码已删，只留下面这些；各留了什么见该目录的 `README.md`）

### 3.1 一定要搬

| 资产 | 路径 | 说明 |
|---|---|---|
| **语料：15 篇文档** | `archive/demo1/corpus/docs/` | 中日混排是刻意设计，跨语言评估靠它 |
| **语料：80 条案例 JSON** | `archive/demo1/corpus/cases/` | 严格符合共享契约 schema，含 15 条刻意植入的脏数据 |
| **语料生成器** | `archive/demo1/tools/gen_cases.py` | 要调整分布时重新生成 |
| **共享契约** | `00-SHARED-CONTEXT.md` §1.2/§1.3 | 领域模型、Action 枚举、8 个模块、8 条 STD 规范 |
| **决策记录全文** | `archive/demo1/DECISIONS.md`（26 条） | 面试素材库。即使代码不用了，坑还是你踩的 |
| **服务机部署与冒烟测试** | `00-SHARED-CONTEXT.md` §2.1 | TEI 两个容器 + Qdrant，含那三个必做的冒烟测试 |
| **面试叙事主线** | `00-SHARED-CONTEXT.md` §4 | 「读 + 写」双 demo 骨架 |

### 3.2 语料的三个设计点（别在重灌时弄丢）

1. **语言分布是刻意的**：手册主体中文，`08` 用日文；STD-001/004/005 用日文；FAQ 中日混排。
   → 支撑评估集 C 类「中文提问召回日文文档」，**这是纯中文 demo 做不出来的展示点**。
2. **80 条案例里植入了约 15 条脏数据**：3 条用 `SLEEP`、4 条绝对路径 XPath、3 条依赖动态 id、
   2 条无断言、3 条 case_code 不合规。
   → 支撑「助手不能无脑推荐存量案例，得能指出『这条可以参考结构，但等待策略违反 STD-004，别照抄』」。
   **这是从「检索」升级到「有判断力的助手」的关键，也是面试记忆点。**
3. **案例 metadata 进 payload**：`module_code, priority, actions_used[], has_violation, violation_codes[], step_count`。

### 3.3 代码：绝大部分不用搬，但有三处值得看一眼再决定

| 组件 | 建议 | 理由 |
|---|---|---|
| `TeiScoringModel`（rerank 适配器） | **看一眼** | 若新引擎自带 rerank 客户端，直接用它的；但要确认两件事：响应 `[{index,score}]` **未排序**、有 **batch 上限 32**。这两个都是静默错误点 |
| `HeadingPathSplitter` / 父子切块 | **看一眼** | 若 MinerU 输出的 markdown 层级干净，新引擎的切分可能已够用。不够时这是现成的参考实现 |
| PDF/DOCX 解析（D-022~D-025） | **不搬** | MinerU 完全覆盖，见 §4.2 |
| Java 8 相关的一切 | **不搬** | 约束不存在了 |

### 3.4 归档但不搬的东西 → 转化成面试素材

demo1 手搓 PDF/DOCX 那四条决策（CJK 字体嵌入、书签坐标系、CTM 操作符、无 outline 降级）
在 v2 里全部作废 —— 但**它们变成了「为什么需要 MinerU」的一手论据**，比二手结论有力：

> 「我先用 PDFBox 自己做过一版：能拿到文本层和书签 outline，
> 移植了 Spring AI 的按 Y 坐标切矩形的算法，三格式 chunk 数能对齐到 199 完全一致。
> 但两个缺口填不上 —— **没有 outline 的 PDF 直接抛异常**（企业里第三方拼接的 PDF 常常没书签），
> 以及**表格超过约 525 字符必被拦腰切断且下半截无表头**。
> 而且 PDF 格式本身不存 alt，图片描述的降级实现榨不出任何语义。
> 这三件事都要视觉模型才能解，所以后来换成了 MinerU。」

**这段话的价值在于「我知道 Java 侧的边界在哪，是量过之后才引入 Python 侧车的」**，
而不是「我用了个流行工具」。

---

## 4. v2 的架构决策

### 4.1 ⭐ 框架统一 —— M0 的第一个决策点

> ⚠️ **2026-08-27 就地修正：这一节的前提已经不成立了。**
> 当时假设生产侧是一个 Spring AI 的 MCP server，所以整节在论证「两个 LLM 框架能不能共存」。
> **MCP 方案已废弃并删除**，生产侧改为 `atp` CLI（`demo2-atp-cli/`）——
> **它全部命令零模型调用，根本不需要 LLM 框架。**
> 下面保留原文是因为"为什么不塞两个框架"的论证本身仍然值得讲，但**结论已改**。

**结论（现行）：一个仓库、Maven 多模块，只有知识侧用 LLM 框架。**

```
atp-ai-demos/
├── atp-common/   领域模型、Action 枚举、配置加载   ← 共享契约从文档落成代码
├── atp-rag/      知识侧（买来的引擎，langchain4j 1.11.0）
└── atp-cli/      生产侧（atp CLI，picocli + JDBC，**无 AI 框架**）
```

⭐ **框架统一这道题现在不存在了。** 不是"两个框架怎么共存"，而是
**生产侧压根不需要框架** —— 模型由 agent 调，CLI 只做确定性的规则与落库。
这个答案比原来任何一版都干净，**而且在面试里更好讲**：

> 「生产侧我没有用任何 LLM 框架。因为在这个设计里 CLI 不调模型 ——
> 调模型的是 agent，CLI 负责的是校验和幂等落库，那是确定性代码。
> 接了框架反而是给一段不需要它的代码增加依赖。」

---

<details>
<summary>原文存档：当时论证「两个框架能不能共存」的三处传递依赖冲突（结论已作废，论证仍可复用）</summary>

**Spring AI 和 langchain4j 能不能一起加载**：技术上能（包名完全不同，
连 `EmbeddingModel` 这种同名接口都是两个不同类型，容器不会互相顶掉），**但不该**。

真正的冲突在传递依赖，三处：

1. **Jackson 分裂** —— Spring Boot 4.1 用 Jackson 3（`tools.jackson`），
   langchain4j 1.x 还在 Jackson 2（`com.fasterxml`）。两套 ObjectMapper 并存不会崩，
   但排查任何序列化问题都要先分清是谁的。
2. **向量库 client / gRPC / protobuf** —— 两边各带一份，版本不齐时爆 `NoSuchMethodError`。
   demo1 的 D-002 已经被一模一样的东西咬过。必须在 `dependencyManagement` 里 pin 一份。
3. 两套 HTTP 栈（RestClient vs okhttp），无功能冲突，纯冗余。

**但不该做的理由不是技术，是叙事**：面试官看到一个应用里塞两个 LLM 框架，
第一个问题是「为什么」，而唯一诚实的答案是「历史原因」—— 在从零搭的 demo 里站不住。

**当时的默认选择是 Spring AI 2.0**，理由之一是「MCP server 是 Spring AI 的主场」。
**这条随 MCP 方案一起作废。**

**当时列的例外分支**：如果买来的项目已经用 langchain4j 深度实现了意图识别和混合检索，
统一到 Spring AI 就等于重写核心 —— 那种情况反过来统一 langchain4j。
→ **`04-M0-engine-audit.md` 查证结论：例外分支成立**，know-engine 是纯 langchain4j 1.11.0 且耦合很深。
所以知识侧留在 langchain4j。

</details>

### 4.2 MinerU 带来的变化（含新增的账要算）

**它一次解决了 demo1 的三个已知缺口**：无 outline 的 PDF、表格拦腰切断、PDF 图片无 alt。

但要算三笔新账：

1. **架构从纯 Java 变成 Java + Python 侧车**。部署多一个服务，要想清楚调用方式
   （HTTP 服务 vs 离线批处理）。**demo 场景建议离线批处理**：语料是固定的，
   转好 markdown 存盘进版本库，查询链路完全不依赖 MinerU 在线 —— 演示时少一个会挂的东西。
2. **显存**。服务机 RTX 5080 是 16G，桌面基线约 1950 MiB，TEI 两个模型加载后实测约 5165 MiB。
   MinerU 的版面/OCR/公式模型要占多少，**必须实测**，不要照抄网上数字。
   若采用离线批处理，可以和 TEI 错峰跑，这笔账就不用算。
3. **判据同 §2.1** —— MinerU 是否真在 GPU 上，看它自己的日志 device 字段 + `nvidia-smi` 显存增量，
   **不要看「能不能 import torch 且 cuda.is_available()」**。demo1 就是在这上面栽过：
   手动进容器测 `cuInit` 成功，但 TEI 二进制的库搜索路径不同，实际还是 CPU。

### 4.3 ATP 语料在新引擎上的三个必查项

买来的引擎是**汽车领域**调出来的，换语料后这三处大概率要重调（理由见 §2.7）：

| 项 | 为什么 | 怎么查 |
|---|---|---|
| **意图识别的关键词/规则表** | ATP 的问法是「点击按钮之前应该用哪种等待策略」这类，和汽车领域完全不同 | 用评估集校准，看路由准确率；补「应该用 / 用哪 / 哪种 / 什么时候 / 有几种 / 有哪些」这类句式 |
| **rerank 截断阈值** | ATP 有「手册 vs 结构化案例」双形态，分数尺度差 1~2 个数量级（§2.2） | 灌数据后立刻量两类内容的 rerank 分数分布 |
| **多语言** | ATP 语料中日混排。若引擎用的是中文 embedding 模型，**日文查询会崩** | 确认 embedding 模型；沿用 bge-m3 最省事（TEI 已部署验证，1024 维，中日英均衡） |

---

## 5. 里程碑

**总纪律：语料一能检索，下一件事就是评估集，不是加功能。**（§2.3）

| # | 里程碑 | 产出 | 卡点 |
|---|---|---|---|
| **M0** | 拉代码 + 摸底 | 跑通原样 demo；搞清框架、切分、意图识别、混检的实现方式；**定框架统一方案**（§4.1） | 不摸清就改，会在别人的假设上叠自己的假设 |
| **M1** | 换语料 | MinerU 把 ATP 的 15 篇文档转 markdown；80 条案例入库；检索能出结果 | §4.3 三个必查项 |
| **M2** | ⭐ 评估集 + baseline | `eval/questions.jsonl` + Recall@5 / MRR@10 + 消融表第 1 行 | **这一步不完成，不准做 M3** |
| **M3** | ⭐ 消融表 3~4 行 | 见下 | |
| **M4** | 合并 demo2 + 演示脚本 | 多模块骨架落地，5 分钟能讲完 | |

### 5.1 消融表（缩到 4 行）

| # | 配置 | Recall@5 | MRR@10 | 拒答率(D类) |
|---|---|---|---|---|
| 1 | baseline：固定切分 + 单 collection + 纯向量 top5 | — | — | — |
| 2 | + 结构感知切分（MinerU 标题层级 → heading path / 父子块） | — | — | — |
| 3 | + 混合检索（BM25 + dense，RRF 融合） | — | — | — |
| 4 | + rerank 精排（召回 N → 精排 5） | — | — | — |

**砍掉的三行**（原计划 7 行）及理由，被问起时照实说：

- 「+ 双 collection 与查询路由」→ 并进第 3 行，混合检索本身已覆盖大部分收益
- 「+ 拒答 prompt 约束」→ 拒答率作为**列**贯穿全表，不单独占行（这样反而更好看）
- 「换 Qwen3-Embedding 对比」→ 成本高（非对称模型要处理 instruction prefix），
  且 40 条样本量下 SOTA 与次优的差距很可能被噪声淹没

**两条实现要求**（demo1 已验证的经验）：

- **每个配置用开关控制，一条命令跑完全部**。不要手动改代码跑 4 遍 ——
  面试官可能会让你现场加一组配置。
- **collection 名必须由配置派生**（如 `atp_all_fixed` / `atp_docs_heading`），不能写死。
  否则跑第 2 组会覆盖第 1 组的数据，**而且不会报错**，
  最后整张消融表只有最后一行是真的。这是 demo1 的 D-005。

### 5.2 评估集（40 条，从零写）

demo1 的 `eval/` 是空目录，这部分要新写。设计沿用：

```json
{
  "qid": "Q001",
  "query": "点击按钮之前应该用哪种等待策略",
  "type": "A",
  "lang": "zh",
  "golden_ids": ["manual/05-等待策略.md#显式等待", "standards/STD-004#CLICK"],
  "note": "考察 STD-005 规则"
}
```

| 类型 | 数量 | 说明 | 考察点 |
|---|---|---|---|
| A. 知识问答 | 15 | 「wait_strategy 有几种」「XPath 能用 class 吗」 | 基础召回 |
| B. 案例检索 | 10 | 「找几个购物车加购的案例」「有没有涉及文件上传的案例」 | 路由 + metadata 过滤 |
| C. 跨语言 | 8 | 中文问 → 日文规范文档 | bge-m3 跨语言能力 |
| D. 应拒答 | 7 | 见 §2.4，**只取光谱两端** | 抗幻觉 |

> `golden_ids` 用 `sourceId#小节标题` 的形式。demo1 验证过：
> 只要三种格式的层级对齐，同一套 `golden_ids` 可以跨格式通用 ——
> 这允许加一组「同样的问题、同样的策略，只换语料格式」的单变量对照。

---

## 6. 开工前的检查清单

```bash
# 1. 三个服务连通（SERVICE_HOST=192.168.0.101）
curl -s http://192.168.0.101:8081/health    # TEI embedding
curl -s http://192.168.0.101:8082/health    # TEI rerank
curl -s http://192.168.0.101:6333/          # Qdrant

# 2. ⚠️ 确认真的在 GPU 上（demo1 踩过，靠 CPU 风扇声才发现）
docker logs tei-embed  2>&1 | grep -i "model on"   # 必须是 Cuda，不能是 Cpu
docker logs tei-rerank 2>&1 | grep -i "model on"

# 3. rerank 打分方向（区分度应达 4 个数量级）
#    ⚠️ 字段是 texts 不是 documents；返回未排序
curl -s http://192.168.0.101:8082/rerank -H 'Content-Type: application/json' -d '{
  "query": "如何编写稳定的 XPath 定位器",
  "texts": ["XPath 应优先使用 data-testid 等稳定属性，避免绝对路径",
            "今天的天气非常好，适合出门散步",
            "购物车结算流程的测试要点"]}' | jq -c 'sort_by(-.score) | .[] | {index, score}'
# 基线：0.735 / 1.6e-5 / 1.6e-5
```

**若 rerank 不通**：设 `RERANK_ENABLED=false` 并在消融表中如实标注该行缺失。
**绝不能拿一个坏掉的 rerank 去跑评估** —— 那会污染整张表，比没有 rerank 严重得多。

> Qdrant 版本约束（v1.11.5）**是 langchain4j 0.35 带来的，v2 不再需要**。
> 用 Spring AI + Java 21 可以升到当前版本，但**仍然要钉死 tag，不要用 `latest`** ——
> demo1 那次踩坑的直接原因就是 `latest`。基础设施不该浮动。

---

## 7. 待确认项（拉下代码后逐条填）

- [ ] 买来的项目：仓库地址 / 本地路径
- [ ] 它用的是 Spring AI 还是 langchain4j？耦合多深？→ 定 §4.1
- [ ] 意图识别是规则、LLM 还是小模型分类？
- [ ] 混合检索的融合方式：RRF 还是加权分数？（影响 §2.2 是否已免疫）
- [ ] 用的什么 embedding 模型？是否支持日文？→ 决定是否换 bge-m3
- [ ] MinerU 的调用方式：在线服务还是离线批处理？显存实测多少？
- [ ] 向量库是什么？沿用还是换 Qdrant（服务机已部署）？
- [ ] 授权状况：能不能公开到 GitHub private 仓库、面试能不能展示 —— **这条要先确认**

> ⚠️ 最后一条不是形式主义。买来的代码有它自己的许可条款，
> 面试展示时的说法也要想好：**「这是我基于一个开源/购买的引擎改造的，
> 我做的部分是 X、Y、Z」** —— 说清楚边界比含糊其辞可信得多，
> 和 demo1 那句「用虚构语料重做的复现 demo」是同一个道理。

---

## 8. 红线（不变）

- 这是**面试用的学习 demo**。全部语料为虚构合成，**不使用任何真实公司资产**。
- 不抓取、不爬取任何真实站点。被测页面一律用本地 mock。
- 不向用户索取任何来自其前雇主的文档、案例或 schema。
- **每个里程碑一个 PR，不要直接推 main**（`https://github.com/Kanash1i/atp-ai-demos`，private）。
  PR 模板里的「怎么验证的」必填。**PR 由用户 review 后 merge，不要自行 merge**。

---

## 9. 新 session 的第一条 prompt

> 读 `/home/kanashi/Applications/interview-demos/03-HANDOFF-rag-v2.md` 和
> `00-SHARED-CONTEXT.md`，然后开始 M0：
> 我买的 RAG 引擎在 `<路径>`，先摸清它的框架、切分、意图识别、混合检索是怎么实现的，
> 给我一份「哪些能直接用、哪些换成 ATP 语料后必须重调」的清单，
> 以及框架统一方案（§4.1）的建议。先不要改代码。
