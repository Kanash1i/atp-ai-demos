<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.1.0</version>
        <relativePath/>
    </parent>

    <groupId>dev.kanashi.atp</groupId>
    <artifactId>atp-mcp-normalizer</artifactId>
    <version>0.1.0-SNAPSHOT</version>
    <name>atp-mcp-normalizer</name>
    <description>ATP 案例规范化 MCP Server</description>

    <properties>
        <java.version>17</java.version>
        <spring-ai.version>2.0.0</spring-ai.version>
        <!--
          L4 本地校验的核心库。MCP SDK 已传递引入 3.0.0，这里显式提到 3.0.6。
          ⚠️ 选版本时必须确认它与 Spring Boot 4.1.0 带的 Jackson 是同一条线：
          3.0.6 依赖 tools.jackson.core:jackson-databind:3.1.4，与本项目一致（见 DECISIONS.md M0-D2）。
          2.x 线是 com.fasterxml 的 Jackson 2，混进来会出现两套 ObjectMapper。
        -->
        <json-schema-validator.version>3.0.6</json-schema-validator.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.ai</groupId>
                <artifactId>spring-ai-bom</artifactId>
                <version>${spring-ai.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <!-- MCP Server over Streamable HTTP (WebMVC)。STATELESS 模式由 application.yml 开启 -->
        <dependency>
            <groupId>org.springframework.ai</groupId>
            <artifactId>spring-ai-starter-mcp-server-webmvc</artifactId>
        </dependency>

        <!--
          L4 的 JSON Schema 校验。这是本服务安全不变式的执行者：
          无论 LLM 走哪种结构化输出策略，这一步永不跳过。
        -->
        <dependency>
            <groupId>com.networknt</groupId>
            <artifactId>json-schema-validator</artifactId>
            <version>${json-schema-validator.version}</version>
        </dependency>

        <!-- k8s liveness / readiness 探针 -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>
</project>
