# 前端契约 — M1 传统功能

> **给谁看**：负责设计稿修订与前端实现的 session。
> **状态**：M1（传统功能读侧）与 **M2（执行链路）** 后端均已完成并实测通过。
> 下面所有响应示例都是**真实 curl 出来的**，不是手写的。
>
> **M2 带来的变化**：执行看板现在有真实数据了 —— 可以派发执行、看进度实时走、点开失败案例看步骤定位、**播放真录像**。
> 详见 §三 面板② 与 §五 演示串联。
> **前置**：设计稿见 Claude Design canvas（Landing / Dashboard 两张画板）。本文只写**与设计稿的差异**和**接口契约**。

起后端：

```bash
./infra/infra.sh up                                  # PG + Redis（在台式机 192.168.0.101）
cd atp-platform && ./scripts/run-web.sh --seed       # 平台 + 导入种子
curl -s localhost:8080/api/health
```

---

## 一、设计稿要改的地方（按重要性排）

### 1. ⭐ 案例详情页去掉 **BROWSER** 和 **TIMEOUT** 两格

稿子的案例详情有 `BROWSER: CHROME` 和 `TIMEOUT: 45 s`。**这两个字段在数据模型里不存在，也不会加。**

- **`browser` 是执行参数，不是案例属性**。案例本身由 `case_type` 区分平台（IOS / ANDROID / PC_WEB），
  而「用哪个浏览器跑」只对 PC_WEB 有意义，且同一条案例本该能在 Chrome 和 Firefox 上各跑一遍。
  钉在案例上既表达不了这件事，还逼着 iOS / Android 案例背一个没意义的字段。
  它的正位在**执行记录**里（`exec_task.browser`），派发时指定。
- **`timeout_sec` 没有消费方**，执行器按步骤各自的 `wait_timeout_sec` 走。

**改法**：这两格换成 `CASE TYPE`（`PC_WEB`）和 `VERSION`（乐观锁版本号，编辑时要带回来）。
想保留"浏览器"这个信息的话，改成显示**最近一次执行用的浏览器**，数据从执行记录取。

### 2. ⭐ 模块归属：`module_code` 全局唯一

稿子把 `M005 用户中心` 同时画在「电商主站」和「管理后台」下，`M001~M004` 又画在「移动端 H5」下。
**这在数据模型里不成立**：`case_code` 的规范是 `ATP-{MODULE}-{4位序号}`（STD-007），MODULE 段直接取 `module_code`，
两个项目复用同一个 code 会让案例编号撞车。

**实际的字典**（12 个模块，`GET /api/projects/{id}/tree` 返回）：

| 项目 | 模块 |
|---|---|
| `P001` EC サイト / 通販フロント | M001 LOGIN、M002 SEARCH、M003 CART、M004 ORDER、M005 USER、M006 PAYMENT |
| `P002` 管理コンソール / 后台管理 | M007 REPORT、M008 ADMIN |
| `P003` モバイル H5 / 移动端 H5 | M009 **MLOGIN**、M010 **MSEARCH**、M011 **MCART**、M012 **MORDER** |

移动端的 code 带 `M` 前缀，是为了不与 PC 端的 LOGIN/CART 撞车。

### 3. ⭐ 执行引擎文案：Selenium Grid → **Playwright Workers**

顶栏的 `ENGINE · Selenium Grid · 6/8 · node-01…node-08`。
执行器实际用 **Playwright for Java**（M2 实现，真跑真录像）。node 池的概念保留，`6/8` 也是真的
（`GET /api/executions/nodes` 返回 8 个节点，6 个在线，在线与否按心跳判定）。

### 4. ⭐ 「RAG 问答助手」→ **智能 Agent 助手**

不只是问答。要对标 gogo-agent 的 `ChatWindow + ProcessingPanel`，一屏里同时容纳：

- 流式对话（SSE）
- **执行进度**（agent 正在做什么、调了哪个工具）
- **工具调用结果卡片**（查到的案例、检索命中的规范）
- **HITL 交互卡片**（确认 / 单选 / 多选 / 表单 —— agent 会主动向用户提问并暂停等待）
- 引用 chip + 检索命中面板（这部分稿子已经画好了，保留）

> 这一屏 M3 才有后端。现在可以先按静态稿实现布局。

### 5. 案例编号与内容不对应，一律**以库为准**

稿子里 `ATP-LOGIN-0002` 是「密码错 3 次锁定账号」，库里是「正确的用户名密码登录，应跳转到首页并显示用户昵称」。
稿子里的 `ATP-CART-0011`（优惠券叠加上限）**在语料里根本不存在**。

审批中心的「规范例外」卡片已经换成 **`ATP-PAYMENT-0001`** —— 它是真的带 `STD-004`（SLEEP）违规的案例，
点进案例详情能看到校验器确实报了这条。稿子那条对不上号。

### 6. 审批卡片的两处文案

- 「经 **MCP 规范化服务**」→ **MCP 方案已废弃并删除**，改成「经 `atp` CLI 提交」或直接去掉。
- 案例变更的 diff `timeout 30 → 60` → timeout 字段没了，改成 `priority P1 → P0`（真实 payload 里就是这个）。

### 7. 三语切换的边界

稿子有 简体 / 日本語 / EN 三语切换。**领域数据不参与 i18n**：

| 不翻译（API 原样返回） | 由前端词条翻译 |
|---|---|
| 项目名、模块名（本身就是日中双语串，如 `ログイン / 登录认证`） | 「案例中心」「执行状态」等导航与标题 |
| 案例标题、步骤的 description | 「通过率」「平均耗时」等指标名 |
| `case_code`、Action 枚举、STD 编号、node 名 | 按钮文案、空状态提示 |
| **规范校验的 message**（后端生成的中文说明） | 表头 |

规范校验的 message 是后端按规则生成的整句中文（例：「第 2 步的 XPath 是从 /html 开始的绝对路径，页面上任何一处结构调整都会让它指向别的元素」）。
它承载的是**判断理由**，不是 UI 文案 —— 三语切换时保持中文，和 STD 编号一样当作领域内容。

### 8. 「执行中的批次」需要一个**空状态**

`GET /api/executions/running` 在没有批次在跑时返回 **204 No Content**，不是 200 加一个空对象。

历史执行数据是种子（撑住统计数字），但**「正在跑」这件事必须是真的** —— 摆一个不动的假进度条，
演示时一刷新就露馅。所以这一格平时是空状态（「当前无执行中的批次」+ 一个「派发执行」按钮），
**现场派发一批它就活起来**（M2 已实现，见 §三 面板②）。

---

## 二、通用约定

### 命名风格：外层 camelCase，`steps` 内部 snake_case

```jsonc
{
  "caseCode": "ATP-ORDER-0001",     // ← camelCase
  "steps": [
    { "locator_type": "XPATH", "wait_strategy": "VISIBLE" }   // ← snake_case
  ]
}
```

**这是刻意的，不是 bug。** `steps` 直接就是 `tc_step.step_json` 里的形状，而那个形状是
**跨四个组件的共享契约**：平台、agent、`atp` CLI（Go）、Playwright 执行器读写同一份 JSON。
在表示层转成 camelCase，等于让 API 返回的步骤和库里存的长得不一样，调试时要多做一次心算。

### 时间

- 格式：`yyyy-MM-dd HH:mm`（列表/详情）、`yyyy-MM-dd HH:mm:ss`（执行记录）
- 时区：**后端已转成 Asia/Tokyo**，前端直接显示，**不要再做时区转换**
- 相对时间（`剩余 21h` / `SLA 超时 +14h`）也由后端算好，前端直接渲染

> 后端踩过一次：列是 `TIMESTAMPTZ`，直接格式化会按 UTC 打，19:49 显示成 10:49 —— 差 9 小时且不报错。
> 所以时间一律在后端统一过一层再出来。

### 错误：RFC 7807 `ProblemDetail`

```json
{ "type":"about:blank", "title":"Not Found", "status":404,
  "detail":"案例不存在：NOPE-123", "instance":"/api/cases/NOPE-123" }
```

| 状态码 | 场景 |
|---|---|
| 404 | 案例 / 审批 / 执行任务不存在 |
| **409** | **审批已被别人处理过**（并发冲突）。`detail` 会说明是谁处理成了什么，前端提示「刷新看看」而不是让用户重试 |
| 400 | 参数不合法 |
| 500 | 兜底，`detail` 带异常类型 |

### 认证

**M1 暂未接** Sa-Token 登录链路。当前用户由查询参数带（`?user=kaneshiro`），默认值就是 `kaneshiro`。
登录接完之后参数保留，用于演示时一键切换身份（提交人视角 ↔ 审批人视角）。

三个演示账号：`kaneshiro`（金城 悠人 / KY / 可审批）、`sato`（佐藤 美咲 / SM）、`tanaka`（田中 直樹 / TN）。

---

## 三、接口契约

Base URL：`http://localhost:8080`

### 面板① 案例中心

```
GET /api/projects                 项目 pill
GET /api/projects/{id}/tree       模块树 + 案例（一次全取，展开/收起是纯客户端行为）
GET /api/cases/{caseId}           案例详情
GET /api/cases/{caseId}/validation 只要校验结果（「重新校验」按钮）
```

**`/api/projects/{id}/tree`** — 一次返回该项目下全部模块与案例，前端不必逐个模块请求：

```json
[{
  "moduleId": "M001", "moduleCode": "LOGIN", "moduleName": "ログイン / 登录认证",
  "caseCount": 10,
  "cases": [{
    "caseId": "73145000000000000",
    "caseCode": "ATP-LOGIN-0001",
    "seqNo": "0001",
    "title": "【历史】登录后等待首页加载完成再校验导航栏",
    "priority": "P2",
    "status": "ACTIVE"
  }]
}]
```

> `seqNo` 是 `case_code` 的后四位，树里显示这个就够（模块名已在父节点上）。
> 但**详情页标题、执行记录、审批一律用完整 `caseCode`**。
> ⚠️ 有 3 条存量案例编号不合规（如 `ATP-ADMIN-0011-V2`），`seqNo` 会返回整串，前端不要假设它总是 4 位数字。

**`/api/cases/{caseId}`** — 详情。`priority` / `status` / `caseType` 都是**字符串枚举名**，不是数字码：

```json
{
  "caseId": "73145000000000030", "caseCode": "ATP-ORDER-0001",
  "title": "【历史】订单列表首行状态校验",
  "moduleId": "M004", "moduleCode": "ORDER", "moduleName": "注文 / 订单管理",
  "projectId": "P001",
  "priority": "P2", "status": "ACTIVE", "caseType": "PC_WEB",
  "author": "tanaka.y", "precondition": null,
  "updatedAt": "2023-06-07 11:30", "version": 0,
  "steps": [
    { "step_id": "…-S01", "case_id": "…", "seq": 1, "action": "OPEN_URL",
      "locator_type": null, "locator_value": null,
      "input_data": "${base_url}/orders", "expected": null,
      "wait_strategy": "NONE", "wait_timeout_sec": 10,
      "on_failure": "ABORT", "description": null },
    { "seq": 2, "action": "ASSERT_TEXT", "locator_type": "XPATH",
      "locator_value": "/html/body/div[1]/div[3]/table/tbody/tr[1]/td[4]/span",
      "expected": "支払い", "wait_strategy": "VISIBLE", "on_failure": "ABORT",
      "description": "※ 规范建立前编写，待改造" }
  ],
  "validation": {
    "passed": false,
    "errorCount": 1, "warnCount": 0, "infoCount": 1,
    "violatedCodes": ["STD-001", "STD-003"],
    "findings": [
      { "std": "STD-001", "severity": "ERROR", "seq": 2,
        "message": "第 2 步的 XPath 是从 /html 开始的绝对路径，页面上任何一处结构调整都会让它指向别的元素" },
      { "std": "STD-003", "severity": "INFO", "seq": 2,
        "message": "第 2 步可以改用 data-testid 定位，比结构和文本都稳" }
    ]
  }
}
```

**规范校验区块怎么渲染**（稿子里那排 STD chips + 「ERROR 0 / WARN 0」）：

- `violatedCodes` → chips。按 `severity` 上色：ERROR 红、WARN 黄、INFO 灰
- `errorCount / warnCount / infoCount` → 那一行计数
- `findings[].seq` → **可以点，高亮到步骤表对应那一行**。`seq` 为 `null` 表示案例级问题（如编号不合规），不指向任何步骤
- `passed: false` → 「保存」按钮禁用。**只有 ERROR 拦人**，WARN/INFO 是建议
  （存量案例里几乎条条都有 STD-003 建议，全拦的话历史数据一条都改不动）

> 步骤表的列（稿子已画好）：SEQ / ACTION / LOCATOR / VALUE / INPUT-EXPECTED / WAIT / ON FAIL。
> `locator_type` 与 `locator_value` 合成 LOCATOR + VALUE 两列；`input_data` 与 `expected` 合成一列。
> ⚠️ 这些字段**大量为 null**（`OPEN_URL` 没有定位器，`CLICK` 没有输入数据），null 要渲染成 `—` 而不是空白或 "null"。

### 面板② 案例执行状态

```
POST /api/executions/dispatch        ⭐ M2 新增：派发执行
GET  /api/executions/stats           顶部四张卡片
GET  /api/executions/running         执行中的批次（204 = 没有）
GET  /api/executions/recent?limit=200 最近执行结果
GET  /api/executions/tasks/{taskId}   失败详情
GET  /api/executions/nodes            节点池（顶栏 6/8）
GET  /api/artifacts/**               ⭐ M2 新增：录像与截图
```

**⭐ `POST /dispatch`** —— 派发一批执行。这是「执行中的批次」那一格从空状态变活的唯一途径。

```jsonc
// 请求
{
  "projectId": "P001",
  "caseIds": ["73145000000000000", "…"],   // 省略或空数组 = 跑该项目下全部案例
  "browser": "CHROME",                      // CHROME | FIREFOX | EDGE
  "suiteName": "回归套件",                   // 自由文本，显示在批次卡片上
  "trigger": "MANUAL",                      // MANUAL | AGENT | SCHEDULED
  "createdBy": "kaneshiro"
}
// 响应
{ "runId": "ac168901-…", "runCode": "RUN-20260830-0015", "totalCount": 20, "status": "RUNNING" }
```

接口**立刻返回**，不等执行完 —— 进度靠轮询 `/running`。

> ⚠️ 没有节点在线时任务会一直挂在队列里，这是**故意**的：任务不会丢，节点起来就接着跑。
> 那时 `/running` 会返回一个 `doneCount: 0` 的批次，而 `/nodes` 里没有 `online: true` 的节点 ——
> **两个信号合起来**才说明问题出在「没有执行节点」，任何单独一个都不够。

**`/running`** —— 派发后轮询它。真实响应：

```json
{
  "runId": "ac168901-2cff-48ca-b523-01e80735df34",
  "runCode": "RUN-20260830-0015",
  "projectName": "EC サイト / 通販フロント",
  "suiteName": "回归套件",
  "browser": "CHROME",
  "triggerSource": "MANUAL",
  "doneCount": 0, "totalCount": 20,
  "passedCount": 0, "failedCount": 0, "skippedCount": 0, "runningCount": 0,
  "elapsedSec": 6, "etaSec": null
}
```

- **轮询 2 秒一次**即可。拿到 **204 就停止轮询**并刷新 `/stats` 与 `/recent`
- `doneCount = passed + failed + skipped`，进度条用它除以 `totalCount`
- `runningCount` 是**此刻正在跑的条数**（实时查询，不是冗余计数），并发节点多时它才 > 0
- ⚠️ **`etaSec` 可能为 `null`** —— 还没有任何任务完成时推算不出来。前端要处理这个空值，
  且有值时也要显示成「≈ 6 分钟」而不是精确值：它是按已完成任务的平均耗时外推的

**⭐ 录像与截图** —— `videoUrl` / `screenshotUrl` 现在是**真实可访问**的：

```
GET /api/artifacts/RUN-20260830-0013/ATP-LOGIN-0002-4fedcd54/a7587e18….webm
→ HTTP 200  Content-Type: video/webm  12765 bytes
```

- 格式是 **webm**（Playwright 原生录制），`<video>` 直接放，**不需要转码**
- 响应带 `Accept-Ranges: bytes`，进度条可以拖
- 尺寸 800×600，单条 200KB~500KB —— 云服务器只有 2Mbps 出站，这是刻意压过的
- ⚠️ **不是每条都有录像**：失败的一定有，成功的约 1/10 抽样。`hasVideo` 为 false 时不要渲染播放器
- ⚠️ 文件不存在时返回 **404**，前端要有兜底（旧记录的产物可能已被清理）

**`/nodes`** —— 顶栏那个「6/8」。

⚠️ 这里混着**两种节点**：`node-01`~`node-03` 是真实执行进程，`node-04`~`node-08` 是种子数据。
前端不需要区分，**只看 `online` 字段**（由心跳判定，不是 `status` 列）。

> 演示时可以当场 `kill` 掉一个真实节点，约 90 秒后看板会显示它掉线，
> 它手上的任务被别的节点接管重跑 —— 这是这一屏最值得展示的动作。

**`/stats`** 与 **`/recent`** 与 **`/tasks/{taskId}`** 的契约不变（见下）。

**`/stats`** — 全部从表里 count 出来，不是常量：

```json
{ "todayTotal": 1284, "passRate": 94.0, "avgDurationSec": 38,
  "failedCount": 76, "failedP0Count": 6,
  "totalDeltaPercent": 7.4, "avgDurationDelta": 0 }
```

- `passRate` 的分母是 `PASSED + FAILED`，**不含 SKIPPED**（跳过的没跑，算进去会把通过率压低）
- `totalDeltaPercent` / `avgDurationDelta` 是与昨日的环比，**可能为 null**（没有昨日数据时），前端要处理
- `avgDurationDelta` 单位是秒，正负都有

**`/recent`** — 「最近执行结果」表：

```json
[{ "taskId": "487c6a6c-…", "caseCode": "ATP-LOGIN-0002",
   "caseTitle": "正确的用户名密码登录，应跳转到首页并显示用户昵称",
   "browser": "FIREFOX", "nodeName": "node-03", "status": "FAILED",
   "duration": "48.4s", "finishedAt": "2026-08-29 19:49:02", "hasVideo": true }]
```

`status`：`PASSED` / `FAILED` / `SKIPPED` / `RUNNING` / `PENDING` / `ABORTED`。
`duration` 已格式化（`48.4s` / `1m 4s`），SKIPPED 为 `null`。
`hasVideo` 为 true 时显示录像图标，点击进详情。

**`/tasks/{taskId}`** — 失败详情。`failedSeq` 直接定位到失败那一步，前端高亮：

```json
{ "taskId": "…", "runCode": "RUN-20260829-0111",
  "caseId": "…", "caseCode": "ATP-LOGIN-0002", "caseTitle": "…",
  "browser": "FIREFOX", "nodeName": "node-03", "status": "FAILED",
  "duration": "48.4s",
  "startedAt": "2026-08-29 19:48:14", "finishedAt": "2026-08-29 19:49:02",
  "failedSeq": 4,
  "errorMsg": "TimeoutError: 页面加载超时，目标环境响应缓慢",
  "videoUrl": "/api/artifacts/RUN-20260829-0111/ATP-LOGIN-0002.webm",
  "screenshotUrl": "/api/artifacts/RUN-20260829-0111/ATP-LOGIN-0002-fail.png",
  "steps": [
    { "seq": 1, "action": "OPEN_URL",    "status": "PASSED",  "duration": "1.0s" },
    { "seq": 4, "action": "ASSERT_TEXT", "status": "FAILED",  "duration": "3.3s",
      "errorMsg": "TimeoutError: …", "screenshotUrl": "/api/artifacts/step-4-fail.png" },
    { "seq": 5, "action": "WAIT_FOR",    "status": "SKIPPED", "duration": null }
  ]
}
```

> 失败之后的步骤是 `SKIPPED`（`on_failure: ABORT` 的效果），渲染成灰色。
> ⚠️ **`videoUrl` / `screenshotUrl` 现在是占位路径，`/api/artifacts/**` 这个端点 M2 才实现**，
> 现在请求会 404。前端先按 `<video>` 布好位，M2 接上就有真文件（Playwright 录的 webm，浏览器原生可播，不需要转码）。
> 只有**失败的**和约 1/10 抽样的成功用例有录像 —— 真实平台不会给每次执行都存视频。

**`/nodes`**：

```json
[{ "nodeName": "node-01", "status": "IDLE", "online": true,
   "currentTaskId": null, "lastHeartbeat": "2026-08-29 20:08:51" }]
```

⚠️ **在线与否看 `online` 字段（由心跳算），不要看 `status`** —— 节点是独立进程，崩了不会回来把自己改成 OFFLINE。
顶栏的 `6/8` = `online === true` 的个数 / 总数。

### 面板③ 审批中心

```
GET  /api/approvals/stats?user=      顶部四个计数
GET  /api/approvals/pending          待审列表（?assignee= 过滤）
GET  /api/approvals/mine?user=       我提交的
GET  /api/approvals/{requestId}      单条
POST /api/approvals/{requestId}/decision
```

**`/stats`** → `{ "awaitingMe": 2, "submittedByMe": 1, "completed": 3, "overdue": 0 }`

**`/pending`** — 按 SLA 紧迫度排序，**超时的排最前**：

```json
[{
  "requestId": "b420474a-…",
  "type": "CASE_CHANGE",
  "targetId": "73145000000000003",
  "title": "ATP-LOGIN-0004 密码错误锁定 —— 新增 3 个断言步骤",
  "summary": "补充锁定后的提示文案与跳转校验",
  "status": "PENDING",
  "submitter": "sato",
  "submittedAt": "2026-08-26 19:48",
  "slaRemaining": "剩余 21h",
  "overdue": false,
  "assignee": "kaneshiro",
  "decidedBy": null, "decidedAt": null, "decisionNote": null,
  "payload": {
    "diff_summary": ["+3 steps", "priority P1 → P0"],
    "standards_check": "PASS",
    "before": { "priority": "P1", "step_count": 5 },
    "after":  { "priority": "P0", "step_count": 8 }
  }
}]
```

`type` 决定卡片样式与 `payload` 的形状，**三类各不相同**：

| type | payload 字段 | 卡片要展示的 |
|---|---|---|
| `RULE_EXCEPTION` | `violated_std`、`step_seq`、`reason`、`expire_at` | 违反哪条 STD、第几步、破例理由、到期日 |
| `CASE_CHANGE` | `diff_summary[]`、`standards_check`、`before{}`、`after{}` | diff 摘要 chips、校验结论、「看差分」展开 before/after |
| `DATASET_RELEASE` | `corpus_name`、`docs_count`、`index_progress`、`evaluated` | 语料集名、文档数、索引进度、是否已评估 |

> `before` / `after` 是**整包快照**（不是变更字段），diff 由前端算 —— 待审期间案例可能又被改了，
> 只存变更字段的话 diff 会对不上。

`overdue: true` 时 `slaRemaining` 形如 `SLA 超时 +14h`，前端标红。`status !== "PENDING"` 时 `slaRemaining` 为 `null`（不再倒计时）。

**`POST /{requestId}/decision`**：

```jsonc
// 请求
{ "decision": "APPROVED", "decidedBy": "kaneshiro", "note": "支付 mock 确实是异步的，批准暂留到 9/30" }
// decision: APPROVED | REJECTED | HOLD
```

返回更新后的完整 `ApprovalVO`。

⚠️ **并发冲突返回 409**：两个审批人同时点，后点的会拿到
`审批 xxx 已经被 kaneshiro 处理为 APPROVED，不能重复决策`。
前端提示「已被他人处理，刷新查看」，**不要自动重试**。

> 三个动作对应稿子上的按钮：批准 = `APPROVED`、退回 = `REJECTED`、挂起 = `HOLD`。
> 退回时 `note` 应当必填 —— 提交人只看得到这一句。

### 面板④ 数据集中心 / 面板⑤ 智能 Agent 助手

**M1 没有后端**。M3 才做（RAG 与 agent 层）。可以先按静态稿实现布局。

数据集中心的表已经建好（`rag_corpus` / `rag_document` / `rag_eval_run`），
向量存储用 pgvector（AgentScope 的 `PgVectorStore` 自建表），embedding 走本地 TEI 的 `bge-m3`。

---

## 四、演示串联（M2 之后能完整跑通的一条线）

这是目前后端支持的完整演示路径，前端按这个顺序把五个面板串起来：

```
① 案例中心
     选中 ATP-ORDER-0001 → 详情页右侧「规范校验」标红 STD-001
     （findings[].seq = 2，点它高亮到步骤表第 2 行的绝对路径 XPath）
        ↓
② 派发执行     POST /dispatch  { projectId, caseIds, browser, suiteName }
        ↓
③ 执行状态     轮询 /running（2 秒），进度条从 0/20 走到 20/20
     顶栏节点池实时反映哪几个节点在干活
        ↓ 拿到 204
④ 刷新 /stats 与 /recent
     「最近执行结果」表出现刚跑的记录，失败的带 🎬 录像图标
        ↓ 点 FAIL
⑤ 失败详情     步骤明细高亮 failedSeq，下面直接播 <video> 录像
```

**一个额外的演示动作**（很值得留在脚本里）：执行途中当场 `kill` 掉一个执行节点，
约 90 秒后节点池显示它掉线，它手上那条任务被别的节点接管重跑，批次照常收尾。
后端已经实现了完整的回收逻辑，前端只要如实展示 `/nodes` 和 `/running` 就行。

**另一个对照**：`ATP-LOGIN-0001` 与 `ATP-LOGIN-0002` 是同一个登录流程，
前者用了规范禁止的 `SLEEP 3`，跑 **4.3 秒**；后者用显式等待，跑 **0.4 秒**。
两条并排放在「最近执行结果」里，十倍差距一眼可见 —— 这是规范存在意义的最好证据。

---

## 四、后端已就绪 / 未就绪一览

| 面板 | 状态 |
|---|---|
| 案例中心（树 / 详情 / 规范校验） | ✅ 可接 |
| 案例执行状态（统计 / 最近结果 / 失败详情 / 节点池） | ✅ 可接 |
| 审批中心（列表 / 统计 / 决策 / 并发冲突） | ✅ 可接 |
| 执行中的批次 | ✅ **可接**（M2）。派发后有真实进度，无批次时 204 |
| 派发执行 `POST /dispatch` | ✅ **可接**（M2） |
| 录像 / 截图 `/api/artifacts/**` | ✅ **可接**（M2）。webm 直接播，带 Range 支持 |
| 案例 CRUD（新建 / 编辑 / 提交审批） | ❌ **M3**（与 agent 的生成路径一起做 —— 人在 UI 上编辑和 agent 生成走同一条写入路径，分开做会变成两套语义）。设计稿上这三个按钮先做出来，点击暂时置灰或提示「即将开放」 |
| 登录（Sa-Token） | ❌ 当前用 `?user=` 参数 |
| 数据集中心 | ❌ M3 |
| 智能 Agent 助手 | ❌ M3 |

种子数据规模：3 个项目 / 12 个模块 / **80 条案例（412 个步骤，其中 15 条带真实规范违规）** /
3 个用户 / 6 条审批 / **36 个执行批次 · 3580 条执行记录**（今日 1284 条）。

**M2 之后可真实执行的案例**：LOGIN 10 条、CART 10 条、ORDER 10 条 —— 共 30 条，
在真实 Chrome 上跑通 **29 条**。唯一失败的 `ATP-ORDER-0003` 不是缺陷：
它与 `ATP-CART-0007` 对购物车初始状态的要求相反（一条要求含缺货商品并被结算拦下，
一条要求没有缺货能进结算页），真实平台靠每条案例独立的测试数据准备解决。
**这条红的是刻意保留的** —— 演示时它比一张全绿的表更能说明测试数据管理的重要性。

其余模块（SEARCH / USER / PAYMENT / REPORT / ADMIN 及移动端）的案例**没有对应的 mock 页面**，
派发它们会全部超时失败。演示时只派发这三个模块。
