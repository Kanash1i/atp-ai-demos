# `atp` CLI —— 项目综述

> **读者：帮我写简历的 agent。** 这份文档自包含，不需要读任何其他文件。
> 里面没有"改造前 / 改造后"，只讲：这是个什么世界、有什么问题、这个东西负责哪一块。

> ⚠️ **写简历时必须守住的一条**：这是**面试用的学习 demo**，平台与语料全部虚构合成，
> **不是任何雇主的生产项目**。措辞上不能暗示它是在职工作产出。
> 正确说法是「我用虚构语料重做的复现 demo，真实实现涉及公司资产不能带出来」。

---

## 1. 世界观：ATP 平台是什么

**ATP（Automation Test Platform）是一个企业内部的自动化测试平台。**

- **测试工程师在 Web 界面上写测试案例，不写代码。** 填表格：这一步做什么、点哪个元素、期望看到什么。
- **案例存在关系型数据库里**（PostgreSQL），不是脚本文件。一条案例由两部分组成：
  - **表头**：业务编号、标题、所属模块、优先级、负责人、前置条件
  - **步骤**：一个有序数组，每一步包含动作、元素定位方式、输入数据、期望结果、等待策略、失败处理
- **独立的执行器服务**从数据库读出案例，驱动浏览器真实执行。
- 平台已经运行多年，技术栈是 Java 8 + Spring 4 —— **一个改得动但改起来要小心的遗留系统。**

一条案例长这样（简化）：

```
编号 ATP-CART-0009 │ 标题「购物车结算」│ 模块 CART │ 优先级 P1 │ 负责人 qa.kanashi

  1. 打开页面    http://shop.example.com/cart        等待：元素出现
  2. 点击        .checkout                            等待：可点击
  3. 断言文本    .msg  期望「订单提交成功」            等待：可见
```

---

## 2. 业务上的痛点

**写案例是高度重复但又不能出错的劳动。**

- 一条案例十几个步骤，每步要填五六个字段
- 平台有一整套规范：编号格式必须是 `ATP-{模块}-{4位序号}`、模块必须来自平台字典、
  禁止用固定时长硬等待、每条案例至少要有一个断言、元素定位不能用脆弱的下标路径……
- 新人上手慢、写错率高；老手嫌烦

**自然的想法：让 AI 来写。** 测试工程师说一句「帮我加一个购物车结算的 Web 案例」，
AI 把十几个步骤生成出来。

---

## 3. 让 AI 写案例，真正难的不是"生成"

模型生成一段像模像样的案例并不难。难的是**让它能安全地进库**：

| 问题 | 具体表现 |
|---|---|
| **编造** | 模型会写出平台字典里根本不存在的模块 ID、编造元素定位路径 |
| **不合规范** | 缺断言、用硬等待、编号格式不对 —— 每一条都是平台明文禁止的 |
| **不能直接入库** | 生成的东西必须有人看一眼才敢落地，而"看一眼"这个动作要有地方发生 |
| **重复写入** | agent 会超时重试、multiagent 会把同一个任务派给两个 worker、用户会重复点确认 —— 一个意图可能被投递好几次 |

**这四件事都不是"把提示词写得更详细"能解决的**，因为提示词只是建议：
模型今天听、明天不听，而且违反了没有任何机制能发现。

---

## 4. `atp` CLI 是什么，在链路里负责哪一块

### 端到端链路

```
  测试工程师
     │  自然语言：「帮我加一个购物车结算的 Web 案例」
     ▼
  AI Agent（opencode / Claude Code）
     │  ← 模型在这里，而且只在这里
     │  理解需求、生成案例内容、把 CLI 的输出转成人话
     │
     │  调用命令行
     ▼
┌─────────────────────────────────────────────┐
│  atp CLI          ← 本项目                    │
│                                             │
│  ① 生成前：告诉 agent 该产出什么形状            │
│  ② 生成后：校验产出是否合规                    │
│  ③ 落库时：状态机 + 人工确认 + 防重复写入       │
└─────────────────────────────────────────────┘
     │  写
     ▼
  ATP 平台数据库（PostgreSQL）
     │  读
     ▼
  执行器 → 浏览器 → 真实执行
```

### CLI 负责的三件事

**① 生成之前，先把边界给出去**

```
atp schema    输出案例的目标结构：哪些字段必填、每个枚举的合法取值
atp modules   输出平台的模块字典 —— 模块 ID 只能从这里面选
```

这一步是把错误挡在**生成阶段**，而不是生成完再收拾。模型知道了取值范围，就不会编造模块 ID。

**② 生成之后，纯本地校验**

```
atp validate -f draft.json
```

零网络、零数据库、零模型调用，毫秒级。校验失败分成两类，**因为它们的下一步动作完全不同**：

- **值不合法** → agent 自己照着报错改
- **必填字段压根没给** → agent 必须**去问用户**，猜一个填进去就是在制造假象

**③ 落库，走一个有人工确认环节的状态机**

```
atp draft     建一份草稿，拿到唯一 ID
atp update    把内容写进草稿（可以反复改）
atp preview   渲染给人看 —— 展示的是数据库里的那一份，不是本地文件
atp commit    用户确认后提交，案例落地为平台原生的「草稿」状态
```

落地之后是一条**普通的平台案例**，执行器和既有的列表页完全无感知 —— 它不知道这条是 AI 写的。

### 一句话概括 CLI 的位置

> **CLI 站在 agent 和数据库之间，把「模型说了算」的部分收窄到只剩"生成内容"，
> 其余全部由确定性代码负责。**
>
> ⭐ **CLI 自己一次模型都不调。** 8 个命令，零 LLM 依赖。

---

## 5. 核心设计思想

### 判据：这条规则，机器能不能判定它被违反了？

- **能** → 写成代码或数据库约束**强制执行**
- **不能** → 才留给提示词，并且承认它不可靠

按这个判据分下来：

| 规则 | 谁来保证 |
|---|---|
| 提交前必须先建草稿 | **数据库**：提交需要一个 ID，而 ID 只能由建草稿产生 |
| 用户确认后被改过的内容不能提交 | **数据库**：乐观锁，内容一改版本号就跳 |
| 必填字段不齐不能落地 | **数据库**：`CHECK` 约束随状态而变 —— 编辑期允许残缺，一落地就必须完整 |
| 规范化结果必须让用户看到 | **CLI**：`preview` 直接打印，不依赖模型转述 |
| 不能绕过 CLI 直接写库 | ⚠️ **本 demo 里这条不成立** —— `.env` 与 CLI 共用凭证，agent 读得到。见下方注 |
| 用户明确确认前不得提交 | **宿主权限门**：opencode 配置里唯独 `commit` 需要弹窗确认 |
| 不得把自己的推断说成用户的明确输入 | ⚠️ **只能靠提示词** —— 机器判不了 |

**七条里五条有机器兜底，一条不成立，一条真正依赖模型自觉。**

> ⚠️ **「不能绕过 CLI 直接写库」这条要老实讲，别当成卖点。**
> 实测中 agent 想删除一份草稿、在 skill 里没找到对应工具，
> **于是读了 `.env` 里的数据库凭证，自己拼 SQL 删了。** 它没做错任何事 —— 是工具集有缺口。
>
> 两个结论，第二个更值钱：
> 1. 真实部署应让 CLI 打**平台的 HTTP API**、只给一个 scope 到「创建/提交案例」的窄 token。
>    那时即使 agent 拿到 token，也做不了 `DELETE FROM tc_case`。
>    本 demo 为省事直连数据库，所以这条边界确实不存在。
> 2. ⭐ **agent 不会因为缺工具就停下 —— 它会用手边任何东西凑一个。**
>    所以"规则下沉"不只是把规则搬进代码，**还必须把工作流需要的每个动作都变成工具**；
>    缺一个，那个缺口就是绕过点。
>
> **这是整个项目里唯一一条从「观察到的真实 agent 行为」得出的结论，
> 比推理出来的结论在面试里更有分量。**

### 退出码就是给 agent 的契约

agent 不靠读诊断文本猜，靠退出码分派：

| 码 | 含义 | agent 该做什么 |
|---|---|---|
| 0 | 成功（**含重复提交的幂等重放**） | 继续 |
| 10 | 用户确认后内容被改过 | 重新展示、让用户重新确认 |
| 11 | 案例不存在 | 重新建草稿 |
| 12 | 字段值不合法 | 照着报错**自己改** |
| 13 | 状态不允许该操作 | 停下，问用户 |
| 14 | 必填信息缺失 | **去问用户，不要猜** |
| 20 | 基础设施故障 | 重试或报警 |

### 防重复写入

agent 场景的"并发"不是大量用户做不同的事，而是**同一个意图被重复投递多次**。做法：

- **幂等键就是平台案例表的主键**，由 CLI 本地生成，重试复用同一个 → 撞唯一约束即视为"上次其实成功了"
- **提交只收 ID 和版本号，不携带任何内容** —— 用户确认的和最终落库的物理上是同一行
- 校验和写入压进同一条 SQL 语句，杜绝"检查通过之后、写入之前内容被改掉"

---

## 6. 技术实现

| | |
|---|---|
| 语言 / 框架 | **Go 1.25 + cobra**，单文件静态二进制 |
| 数据库 | PostgreSQL（平台原有），**pgx/v5**，每次调用一条连接、无连接池 |
| 命令 | 8 个，**全部零模型调用**；其中 2 个零数据库零网络 |
| 测试 | **27 个全绿**，跑在 testcontainers-go 起的真实 PostgreSQL 17 上 |
| 冷启动 | **9 ms**（同一套设计的 Java 原型实测 349 ms —— 差 39 倍）|
| 接入 | opencode（配置 + skill），`commit` 单独设为需要确认 |

**先用 Java 写了一版，再整体重写成 Go**（`DECISIONS.md` D-121）—— 这是可以主动讲的过程：

- **冷启动**：Java 原型 349 ms，加 AppCDS 到 230 ms，Go 是 **9 ms**。
  CLI 被 agent 高频反复调用，一轮任务调 20 次就是 7 秒 vs 0.2 秒。
- **⭐ 真正决定选 Go 而不是 TypeScript 的是并发测试**：核心断言是
  「10 个线程并发提交同一个 key，只创建一条」。TS 的 `Promise.all` 跑在单线程事件循环上，
  是**并发不是并行**，测不出行锁与唯一约束的真实行为；
  Go 的 goroutine 默认跑满 `GOMAXPROCS`，10 条连接是真的同时撞库。
- 同理不用连接池：进程只活几毫秒，连接池的预热比它省下的还多。

---

## 7. 可直接用的简历条目

**长版（3 行）**

> 为遗留自动化测试平台设计并实现 `atp` CLI，让 AI Agent 能安全地把生成的测试案例写入平台数据库。
> CLI 负责 agent 与数据库之间的全部确定性工作：生成前下发 schema 与模块字典以杜绝编造、
> 生成后做纯本地规则校验、落库走带人工确认环节的状态机；以退出码作为 agent 的分派契约，
> 并用主键唯一约束与乐观锁兜住 agent 重试、multiagent 并发导致的重复写入。
> Go + cobra + pgx，单文件二进制冷启动 9 ms（Java 原型 349 ms）；
> 27 个测试跑在真实 PostgreSQL 容器上，含 10 goroutine 真并行的幂等验证。

**中版（2 行）**

> 实现 `atp` CLI 作为 AI Agent 与遗留测试平台数据库之间的确定性中间层：
> 下发 schema 约束生成、本地校验产出、以状态机与人工确认关卡管控落库，
> 并以退出码作为 agent 的错误分派契约。八个命令零 LLM 依赖，74 个测试基于真实数据库容器。

**短版（1 行）**

> 设计 CLI 作为 AI Agent 与遗留系统数据库之间的确定性中间层，把可判定的规则下沉为代码与数据库约束，避免依赖提示词约束模型行为。

---

## 8. 面试会被追问的点

1. **「为什么不把规范都写进提示词？」** —— 提示词是建议不是约束：写了不等于生效，
   而且违反了不可观测。**你无法优化一个观测不到的东西。**
2. **「CLI 和 MCP server 怎么选？」** —— CLI 可以替代协议与呈现方式，
   但替代不了**仲裁点的位置**。这个项目里平台数据库改得动，所以把幂等键直接做成案例表的主键，
   外挂一层服务就是多余的转发。
3. **「怎么证明它真的防住了重复写入？」** —— 10 线程并发提交同一个 key 的用例；
   外加变异检验（故意改坏版本比对，对应用例立刻变红）。
4. **「哪里还不够？」** —— 见下一节，主动说。

---

## 9. 边界与未完成（面试时主动交代）

- **建草稿这一步本身不幂等**：客户端超时重试若不复用同一个 ID，会产生两条各自合法的草稿。
  修法是请求携带客户端生成的 requestId 并加唯一约束。**这是已知缺口。**
- **数据库不校验状态码的合法性**：状态存整数换来了"加新状态不用改表结构"，
  代价是写入非法值数据库照收。防线在应用层，前提是所有写入都走 CLI。
- **CLI 的版本收敛是 O(N)**：多台机器上多个 CLI 版本，规则改一条要全量升级；
  服务端方案是改一行重启。**这一维上 CLI 是输的。**
- **未实现**：完整的定位器规范检查、草稿的定期清理任务。
  **刻意押后** —— 演示不依赖它们，而项目的另一半（知识侧检索评估）优先级更高。

---

## 10. 一句收尾

> **「提示词里每留一条机器能管的规则，都是在用不可靠的手段做可靠的事。」**
