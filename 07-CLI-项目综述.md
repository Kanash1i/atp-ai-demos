# `atp` CLI —— 项目综述

> **这份文档是给「帮我写简历的 agent」看的**，自包含，不需要读其他文档。
> 设计细节见 `05-CLI-并发幂等答辩稿.md`（面试口述稿）与 `06-atp-cli-设计.md`（实现设计）。

> ⚠️ **写简历时必须守住的一条**：这是**面试用的学习 demo**，语料与平台全部虚构合成，
> **不是任何雇主的生产项目**。措辞上不能暗示它是在职工作产出。
> 正确说法是「我用虚构语料重做的复现 demo，真实实现涉及公司资产不能带出来」——
> **这个说法本身在面试里是加分项，它证明职业操守。**

---

## 1. 一句话

> 把「让 agent 按平台规范写测试案例」这件事，**从「写一份超详细的提示词」改成「给它一个 CLI」** ——
> 凡是机器能判定的规则，全部下沉成确定性代码强制执行，提示词里只留机器判不了的。

---

## 2. 要解决的问题

原方案是把平台规范、字段约束、操作流程全写进 `SKILL.md`（agent 的提示词），靠模型记住并遵守。
四个具体毛病：

| # | 毛病 | 具体表现 |
|---|---|---|
| 1 | **占上下文** | 规范越全，提示词越长，而它每一轮对话都在 context 里 |
| 2 | **遵守与否不可验证** | 模型今天听、明天不听，没有任何机制能发现 |
| 3 | **出错不可归因** | 案例写错了，不知道是哪一条规则没生效 —— 提示词没有"执行日志" |
| 4 | **对模型能力要求高** | 十几条约束同时遵守，弱模型直接崩；换模型就要重调提示词 |

第 3 条是最致命的：**你无法优化一个观测不到的东西。** 想改进只能靠猜，改完也不知道有没有变好。

---

## 3. 做法

### 3.1 判据

> **「这条规则，机器能不能判定它被违反了？」**
>
> - **能** → 下沉到 CLI / 数据库强制执行，然后**从提示词里删掉**
> - **不能** → 留在提示词里，并且承认它不可靠

提示词里每留一条机器能管的规则，都是**在用不可靠的手段做可靠的事**。

### 3.2 规则下沉清单（这是本项目的核心产出）

| 原提示词里的约束 | 现在谁来保证 | 违反时的表现 |
|---|---|---|
| 「不得跳过草稿准备直接提交」 | **数据库**：提交需要一个 id，而 id 只能由 `draft` 产生，且状态必须是编写态 | 退出码 11 / 13 |
| 「不得提交用户已修改过的旧版本」 | **数据库**：乐观锁，内容一改版本号就跳 | 退出码 10 |
| 「必填字段不齐不能提交」 | **数据库**：`CHECK` 约束随状态而变，编辑期允许残缺，一离开编辑态就必须完整 | 退出码 12 |
| 「规范化结果必须在确认摘要里说明」 | **CLI**：`atp preview` 直接把内容打出来，不依赖模型转述 | 用户直接看到 |
| 「不得直接执行数据库 SQL」 | **凭证**：agent 手上根本没有数据库凭证，只有一个能调 CLI 的环境 | 无法执行 |
| 「用户明确确认前不得提交」 | **宿主权限门**：opencode 配置里唯独 `commit` 是 `ask`，其余 `allow` | 弹窗拦截 |
| 「不得把自己的推断说成用户的明确输入」 | ⚠️ **仍靠提示词** —— 机器判不了 | 无法检测 |

**结果：靠模型自觉的规则从 5 条降到 1~2 条。**

> ⚠️ **不要用「提示词变短了」当卖点** —— 实测新的 `SKILL.md` 是 39 行，
> 比原来的 31 行**还长**。多出来的是**退出码查表**（确定性分派），
> 恰恰是"不需要模型记住、照着做就行"的那类内容。
> 真正的变化是**性质**：从「靠记忆遵守」变成「靠查表分派」。

### 3.3 退出码即契约

agent 不再靠读诊断文本猜下一步，而是靠退出码分派：

| 码 | 含义 | agent 该做什么 |
|---|---|---|
| 0 | 成功（**含幂等重放**） | 继续 |
| 10 | 版本冲突：内容在用户确认后被改过 | 重新展示、让用户重新确认 |
| 11 | 不存在 | 重新建草稿 |
| 12 | 值不合法 | 读 violations **自己改** |
| 13 | 状态冲突 | 停下，问用户 |
| 14 | 缺必填信息 | **去问用户，不要猜** |
| 20 | 基础设施故障 | 重试或报警 |

**两条不能动的**：

1. **幂等重放必须返回 0** —— 重放在语义上是成功，返回非 0 会让 agent 无限重试
2. **12 和 14 必须分开** —— 一个是 agent 自己能改，一个必须去问人。
   **下一步动作不同的，就不能合并成一个码。**

---

## 4. 成果与数字（全部实测，可复现）

| 项 | 数字 |
|---|---|
| 子命令 | 8 个（`schema` / `modules` / `validate` / `draft` / `show` / `update` / `preview` / `commit`）|
| 其中**零模型调用** | **8 个 —— 全部**。模型由 agent 调，CLI 只做确定性的校验与落库 |
| 其中零 DB、零网络 | 2 个（`schema` / `validate`），毫秒级，可无限并发调用 |
| 主代码 / 测试 / DDL | 2064 / 1067 / 177 行 |
| 自动化测试 | **74 个全绿**（真 PostgreSQL 17，Testcontainers 起容器） |
| 冷启动 | `atp --version` **0.34 s**，`atp validate` **0.45 s**（对比 Spring Boot ≈1.5 s）|
| 决策记录 | 21 条（`DECISIONS.md`，含 7 条"静默失败"类踩坑） |

### 一个反直觉的技术取舍

**这个 Java 项目刻意不用 Spring。** 因为 CLI 被 agent 高频反复调用，冷启动是真实成本：
一次会话调 20 次，Spring Boot 是 30 s，picocli fat jar 是 6 s。
同理不用连接池 —— 进程只活 300 ms，HikariCP 的预热比它省下的还多。

> 面试里这是个好切口：**「我是 Spring 工程师，但这里我没用 Spring，因为 CLI 的性能画像和 web 服务完全不同。」**

---

## 5. 次要设计：并发幂等（防脏写，不是主线）

⚠️ **写简历时不要把这块当主线** —— 它是为了兜住 agent 场景的一个具体风险，不是项目目的。

**风险来源**：agent 场景的"高并发"不是大量用户做不同的事，而是**同一个逻辑意图被重复投递多次** ——
LLM 客户端超时重试、agent 自身的 retry loop、multiagent fan-out、用户重复点确认。

**做法**：

- **幂等键做成平台案例表的主键**，由 CLI 本地生成 UUID，重试复用同一个 → 撞唯一约束即幂等重放
- **提交只收 id + 版本号，不带任何内容** —— 用户确认的和最终落库的物理上是同一行
- **检查与写入压进同一条 UPDATE**（状态和版本都写进 `WHERE`），杜绝"检查通过后、写入前内容被改"
- **编辑期只写一张表一行**，最高频的路径不跨表；跨表只发生在提交那一次

**证据**：10 线程并发提交同一个 key → 只创建一条，其余 9 个 `replayed=true` 且退出码 0。
变异检验：把版本比对从 `= ?` 改成 `>= ?`，对应用例立刻变红（证明测试有牙齿）。

---

## 6. 可直接用的简历条目

**长版（3 行）**

> 设计并实现 `atp` CLI，替代「把平台规范全写进 agent 提示词」的做法：
> 以「机器能否判定规则被违反」为判据，把状态机、乐观锁、必填校验等约束下沉到 CLI 与数据库强制执行，
> 提示词中靠模型自觉的规则由 5 条降至 1~2 条，规则违反从"不可观测"变为"可分派的退出码"。
> Java 21 + picocli，刻意不用 Spring（冷启动 0.34 s vs 1.5 s），74 个自动化测试基于真实 PostgreSQL 容器。

**中版（2 行）**

> 把 agent 的「超长提示词 + 靠模型遵守」改造成「CLI + 数据库约束强制」，
> 以退出码作为 agent 的分派契约；靠模型自觉的规则由 5 条降至 1~2 条，
> 并用主键唯一约束与 CAS 兜住 agent 重试与 multiagent 并发导致的重复写入。

**短版（1 行）**

> 用 CLI 替代超长提示词驱动 agent 完成结构化写库任务，把可判定的规则下沉为代码与数据库约束，降低对模型能力的依赖。

---

## 7. 面试会被追问的点（准备好这几个）

1. **「为什么不用 MCP server？」** —— 这个方案的前身就是 MCP server，被废弃了。
   核心论证：CLI 可以替代协议和呈现，替代不了**仲裁点的位置**；
   而既然平台改得动，把幂等键直接做成平台案例表的主键，外挂的那张表就是多余的一层转发。
2. **「提示词不是也能写这些规则吗？」** —— 能写，但**写了不等于生效**，且违反不可观测。
   判据就是那一条：机器能不能判定它被违反了。
3. **「你怎么证明它真的幂等？」** —— 10 线程并发用例 + 变异检验。
4. **「哪里还不够？」** —— 见下一节，主动说。

---

## 8. 边界与未完成（面试时主动交代，不要等对方问出来）

- **`atp draft` 本身不幂等**：客户端超时重试若不复用同一个 UUID，会产生两条各自合法的草稿。
  修法是请求携带客户端生成的 requestId 并加唯一约束。**这是已知缺口。**
- **数据库不校验枚举码的合法性**：枚举存 `SMALLINT` 换来了"加状态不用改表结构"，
  代价是 `status = 99` 数据库照收。防线在应用层，前提是所有写入都走 CLI 或平台代码。
- **CLI 的版本收敛是 O(N)**：20 台机器 20 个版本，规则改一条要全量升级；server 是改一行重启。
  **这一维上 CLI 输给 server。**
- **未实现**：完整规则引擎（定位器规范检查等）与草稿清理定时任务。
  **刻意押后** —— 演示不依赖它们，而项目的另一半（知识侧检索评估）优先级更高。

---

## 9. 一句收尾

> **「提示词里每留一条机器能管的规则，都是在用不可靠的手段做可靠的事。」**
