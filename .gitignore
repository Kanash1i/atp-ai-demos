# 密钥与本地配置 —— 绝不提交
.env
.env.local
*.key
*.pem

# 构建产物
target/
build/
*.class
*.jar
!.mvn/wrapper/maven-wrapper.jar

# IDE
.idea/
*.iml
.vscode/
.settings/
.classpath
.project

# 日志与临时文件
*.log
logs/
tmp/

# 评估输出（结果表建议手动挑选后提交）
eval/results/raw/

# Claude Code
# 两个 demo 都以后台任务开发，会在此创建 worktree —— 不该进版本库
.claude/worktrees/
.claude/settings.local.json
# 注意：.claude/settings.json 是项目级共享配置，**可以**提交，故不在此忽略

# 从 PDF / DOCX 抽出来的原图 —— 运行产物，由 ingest 重新生成。
# 生成的 PDF/DOCX 语料本身**要提交**（clone 下来就能跑三格式对照），
# 但抽出的图不必：它们只是语料里那张图的副本。
demo1-atp-rag/corpus/extracted/

# opencode 插件依赖（跑 opencode 时自动装的，不进仓库）
node_modules/
